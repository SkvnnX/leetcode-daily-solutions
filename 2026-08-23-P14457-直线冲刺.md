# 2026-08-23｜P14457. 直线冲刺

- 题号：P14457
- 题名：直线冲刺
- 分值：100 分
- 难度：简单
- 标签：模拟
- 来源：CodeFun2000 / 华为 OD
- 原题链接：https://codefun2000.com/new-p/P14457

## 题意概括

给定一条长度为 `n` 的直线跑道，坐标从 `0` 到 `n - 1`。数组 `tiles` 表示每个格子的类型：

- `0`：普通空地，下一步向右移动 `1` 格；
- `1`：弹簧加速垫，下一步向右移动 `2` 格；
- `-1`：陷阱，踩到后失败。

角色从坐标 `0` 出发，只能向右移动。如果能够到达或越过终点坐标 `n - 1`，输出通关所需步数；否则输出 `-1`。

## 关键思路

这题没有选择分支：每一步能走多远完全由当前格子的值决定。

因此直接模拟即可：

1. 令 `pos = 0` 表示当前位置，`steps = 0` 表示已走步数。
2. 只要 `pos < n - 1`，说明还没到终点。
3. 如果 `tiles[pos] == -1`，说明踩到陷阱，直接返回 `-1`。
4. 如果当前格子是 `1`，向右走 `2` 格；否则向右走 `1` 格。
5. 每移动一次，步数加 `1`。
6. 当 `pos >= n - 1` 时，说明成功通关，返回步数。

## 复杂度分析

- 时间复杂度：`O(n)`，最坏情况下每次只前进一步。
- 空间复杂度：`O(1)`，只使用常数变量。

## 容易踩坑点

1. **陷阱只在落到该格时失败**  
   如果弹簧直接跳过陷阱，不算踩到陷阱。

2. **到达或越过终点都算成功**  
   判断条件应是 `pos >= n - 1`。

3. **起点就是终点时步数为 0**  
   当 `n == 1`，无需移动。

## C++ 解法

```cpp
#include <vector>
using namespace std;

class Solution {
public:
    int sprint(vector<int>& tiles) {
        int n = tiles.size();
        int pos = 0;
        int steps = 0;

        while (pos < n - 1) {
            if (tiles[pos] == -1) {
                return -1;
            }

            if (tiles[pos] == 1) {
                pos += 2;
            } else {
                pos += 1;
            }
            ++steps;
        }

        return steps;
    }
};
```

## Python 3 解法

```python
from typing import List


class Solution:
    def sprint(self, tiles: List[int]) -> int:
        n = len(tiles)
        pos = 0
        steps = 0

        while pos < n - 1:
            if tiles[pos] == -1:
                return -1

            pos += 2 if tiles[pos] == 1 else 1
            steps += 1

        return steps
```

## ACM 答题模式

输入为一个数组，例如：

```text
[0,1,-1,0,0]
```

输出一个整数，表示通关步数；无法通关则输出 `-1`。

### C++ ACM 模式

```cpp
#include <iostream>
#include <regex>
#include <string>
#include <vector>
using namespace std;

int solve(vector<int>& tiles) {
    int n = tiles.size();
    int pos = 0;
    int steps = 0;

    while (pos < n - 1) {
        if (tiles[pos] == -1) return -1;
        pos += tiles[pos] == 1 ? 2 : 1;
        ++steps;
    }

    return steps;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string input, line;
    while (getline(cin, line)) input += line;

    regex num_re("-?\\d+");
    vector<int> tiles;
    for (sregex_iterator it(input.begin(), input.end(), num_re), end; it != end; ++it) {
        tiles.push_back(stoi(it->str()));
    }

    cout << solve(tiles) << '\n';
    return 0;
}
```

### Python 3 ACM 模式

```python
import re
import sys


def solve(tiles):
    n = len(tiles)
    pos = 0
    steps = 0

    while pos < n - 1:
        if tiles[pos] == -1:
            return -1
        pos += 2 if tiles[pos] == 1 else 1
        steps += 1

    return steps


def main():
    nums = list(map(int, re.findall(r"-?\d+", sys.stdin.read())))
    print(solve(nums))


if __name__ == "__main__":
    main()
```

## 示例验证

```python
s = Solution()
assert s.sprint([0, 1, -1, 0, 0]) == 3
assert s.sprint([1, 0, -1, 0]) == -1
```

## 小结

本题重点是明确“下一步长度由当前格子决定”。按规则从左到右模拟，遇到陷阱失败，到达或越过终点成功。
