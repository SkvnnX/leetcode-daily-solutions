# 2026-08-23｜P14458. 小牛牛超市选品

- 题号：P14458
- 题名：小牛牛超市选品
- 分值：100 分
- 难度：简单
- 标签：双指针、滑动窗口
- 来源：CodeFun2000 / 华为 OD
- 原题链接：https://codefun2000.com/new-p/P14458

## 题意概括

牛牛在超市采购商品，每件商品有一个正整数价格。给定预算上限 `limit`，需要选出一段**连续商品**，同时满足：

1. 这段商品的总价格严格大于 `limit`；
2. 这段商品中的最贵商品价格小于等于 `limit`。

要求返回满足条件的最短连续商品长度；如果不存在合法区间，输出 `0`。

## 关键思路

由于商品价格都是正整数，可以使用滑动窗口。

第二个条件“区间最大值 `<= limit`”等价于：窗口内不能出现任何大于 `limit` 的商品。一旦遇到 `price > limit`，包含它的所有区间都非法，所以直接清空窗口，从下一个位置重新开始。

对于连续合法段内的商品，所有价格都 `<= limit`，只需要找最短子数组，使得区间和 `> limit`：

1. 右指针不断加入商品，累加窗口和。
2. 当遇到价格 `> limit` 的商品，重置窗口。
3. 当窗口和 `> limit` 时，当前窗口合法，更新答案。
4. 为了找更短长度，持续移动左指针缩小窗口，直到窗口和不再大于 `limit`。

## 复杂度分析

- 时间复杂度：`O(n)`，左右指针都最多移动 `n` 次。
- 空间复杂度：`O(1)`，只使用常数变量。

## 容易踩坑点

1. **总价格必须严格大于 `limit`**  
   等于 `limit` 不合法。

2. **单个商品价格大于 `limit` 时不能进入窗口**  
   因为它会导致“最贵商品价格 `<= limit`”不成立。

3. **价格为正整数是滑动窗口成立的关键**  
   因为窗口右扩时和增加，左缩时和减少。

## C++ 解法

```cpp
#include <algorithm>
#include <climits>
#include <vector>
using namespace std;

class Solution {
public:
    int minSelectedLength(int n, int limit, vector<int>& prices) {
        int left = 0;
        int sum = 0;
        int ans = INT_MAX;

        for (int right = 0; right < n; ++right) {
            if (prices[right] > limit) {
                left = right + 1;
                sum = 0;
                continue;
            }

            sum += prices[right];

            while (sum > limit) {
                ans = min(ans, right - left + 1);
                sum -= prices[left];
                ++left;
            }
        }

        return ans == INT_MAX ? 0 : ans;
    }
};
```

## Python 3 解法

```python
from typing import List


class Solution:
    def minSelectedLength(self, n: int, limit: int, prices: List[int]) -> int:
        left = 0
        total = 0
        ans = float("inf")

        for right, price in enumerate(prices):
            if price > limit:
                left = right + 1
                total = 0
                continue

            total += price

            while total > limit:
                ans = min(ans, right - left + 1)
                total -= prices[left]
                left += 1

        return 0 if ans == float("inf") else ans
```

## ACM 答题模式

输入格式按原题样例可写成一行：

```text
n,limit,[A0,A1,...,A(n-1)]
```

例如：

```text
6,7,[2,3,1,2,4,3]
```

输出合法最短区间长度；无合法区间输出 `0`。

### C++ ACM 模式

```cpp
#include <algorithm>
#include <climits>
#include <iostream>
#include <regex>
#include <string>
#include <vector>
using namespace std;

int solve(int n, int limit, vector<int>& prices) {
    int left = 0;
    int sum = 0;
    int ans = INT_MAX;

    for (int right = 0; right < n; ++right) {
        if (prices[right] > limit) {
            left = right + 1;
            sum = 0;
            continue;
        }

        sum += prices[right];

        while (sum > limit) {
            ans = min(ans, right - left + 1);
            sum -= prices[left];
            ++left;
        }
    }

    return ans == INT_MAX ? 0 : ans;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string input, line;
    while (getline(cin, line)) input += line;

    regex num_re("-?\\d+");
    vector<int> data;
    for (sregex_iterator it(input.begin(), input.end(), num_re), end; it != end; ++it) {
        data.push_back(stoi(it->str()));
    }

    int n = data[0];
    int limit = data[1];
    vector<int> prices(data.begin() + 2, data.begin() + 2 + n);

    cout << solve(n, limit, prices) << '\n';
    return 0;
}
```

### Python 3 ACM 模式

```python
import re
import sys


def solve(n, limit, prices):
    left = 0
    total = 0
    ans = float("inf")

    for right, price in enumerate(prices):
        if price > limit:
            left = right + 1
            total = 0
            continue

        total += price

        while total > limit:
            ans = min(ans, right - left + 1)
            total -= prices[left]
            left += 1

    return 0 if ans == float("inf") else ans


def main():
    data = list(map(int, re.findall(r"-?\d+", sys.stdin.read())))
    n, limit = data[0], data[1]
    prices = data[2:2 + n]
    print(solve(n, limit, prices))


if __name__ == "__main__":
    main()
```

## 示例验证

```python
s = Solution()
assert s.minSelectedLength(6, 7, [2, 3, 1, 2, 4, 3]) == 3
assert s.minSelectedLength(3, 11, [1, 2, 3]) == 0
```

## 小结

本题本质是“正整数数组中找和大于 `limit` 的最短连续子数组”，但要额外处理价格超过 `limit` 的商品：它不能出现在任何合法窗口里，因此遇到后直接重置窗口。
