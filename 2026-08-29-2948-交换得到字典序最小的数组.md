# 2026-08-29｜2948. 交换得到字典序最小的数组

- 题号：2948
- 题名：交换得到字典序最小的数组
- 英文题名：Make Lexicographically Smallest Array by Swapping Elements
- 难度：中等 / Medium
- 标签：数组、并查集、排序
- 链接：https://leetcode.cn/problems/make-lexicographically-smallest-array-by-swapping-elements/

## 题意概括

给定一个正整数数组 `nums` 和正整数 `limit`。一次操作中，可以选择两个下标 `i` 和 `j`，当且仅当 `|nums[i] - nums[j]| <= limit` 时，可以交换 `nums[i]` 和 `nums[j]`。

可以执行任意多次操作，要求返回能得到的**字典序最小**数组。

## 关键思路

这题的核心是判断哪些元素之间可以通过若干次交换互相到达。

如果两个数的差值不超过 `limit`，它们可以直接交换；如果 `a` 能和 `b` 交换，`b` 又能和 `c` 交换，那么即使 `a` 和 `c` 的差值可能大于 `limit`，它们也可以通过中转交换，最终在同一个连通块内重新排列。

因此问题可以转化为：

1. 把 `(数值, 原下标)` 按数值从小到大排序。
2. 在排序后的序列中，如果相邻两个数的差值 `<= limit`，它们属于同一组。
3. 一旦相邻差值 `> limit`，说明无法跨过这个断点，前后属于不同组。
4. 对每一组：
   - 组内的值可以任意重新分配到组内下标。
   - 为了字典序最小，应该把组内下标升序排列，把组内数值升序排列，然后小值放到小下标上。

为什么只看排序后相邻差值就够？因为如果排序后某两个相邻值的差值大于 `limit`，那么左边所有值都更小，右边所有值都更大，跨过这个断点的任意两个值差值都会更大，所以一定不能连通。反过来，如果一段内相邻差值都不超过 `limit`，就能通过链式交换连成一个整体。

## 复杂度分析

- 时间复杂度：`O(n log n)`  
  主要来自排序。每个元素只会被分组处理一次。

- 空间复杂度：`O(n)`  
  需要保存排序后的 `(值, 下标)` 以及答案数组。

## 容易踩坑点

1. **不是只交换差值直接小于等于 limit 的元素对**  
   可交换关系具有传递性，要考虑连通块。

2. **组内要按原下标升序放值**  
   字典序优先看前面的下标，所以小值应该尽量放到组内更小的下标上。

3. **排序后遇到断点才分组**  
   判断断点用的是相邻排序值之差，而不是原数组相邻值之差。

## C++ 解法

```cpp
#include <algorithm>
#include <vector>
using namespace std;

class Solution {
public:
    vector<int> lexicographicallySmallestArray(vector<int>& nums, int limit) {
        int n = nums.size();
        vector<pair<int, int>> arr;
        arr.reserve(n);

        for (int i = 0; i < n; ++i) {
            arr.push_back({nums[i], i});
        }

        sort(arr.begin(), arr.end());

        vector<int> ans(n);
        int start = 0;

        while (start < n) {
            int end = start + 1;

            while (end < n && arr[end].first - arr[end - 1].first <= limit) {
                ++end;
            }

            vector<int> indices;
            indices.reserve(end - start);

            for (int k = start; k < end; ++k) {
                indices.push_back(arr[k].second);
            }

            sort(indices.begin(), indices.end());

            for (int k = 0; k < (int)indices.size(); ++k) {
                ans[indices[k]] = arr[start + k].first;
            }

            start = end;
        }

        return ans;
    }
};
```

## Python 3 解法

```python
from typing import List


class Solution:
    def lexicographicallySmallestArray(self, nums: List[int], limit: int) -> List[int]:
        n = len(nums)
        arr = sorted((num, i) for i, num in enumerate(nums))
        ans = [0] * n

        start = 0
        while start < n:
            end = start + 1

            while end < n and arr[end][0] - arr[end - 1][0] <= limit:
                end += 1

            indices = sorted(idx for _, idx in arr[start:end])
            values = [num for num, _ in arr[start:end]]  # arr 已经按值升序

            for idx, value in zip(indices, values):
                ans[idx] = value

            start = end

        return ans
```

## ACM 答题模式

LeetCode 默认只需要写 `Solution` 类；如果是在 ACM / 笔试平台，需要自己处理标准输入输出。这里约定输入格式为：

```text
n limit
nums[0] nums[1] ... nums[n-1]
```

输出字典序最小数组，元素之间用空格分隔。

### C++ ACM 模式

```cpp
#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

vector<int> solve(vector<int>& nums, int limit) {
    int n = nums.size();
    vector<pair<int, int>> arr;
    arr.reserve(n);

    for (int i = 0; i < n; ++i) {
        arr.push_back({nums[i], i});
    }

    sort(arr.begin(), arr.end());

    vector<int> ans(n);
    int start = 0;

    while (start < n) {
        int end = start + 1;

        while (end < n && arr[end].first - arr[end - 1].first <= limit) {
            ++end;
        }

        vector<int> indices;
        indices.reserve(end - start);

        for (int k = start; k < end; ++k) {
            indices.push_back(arr[k].second);
        }

        sort(indices.begin(), indices.end());

        for (int k = 0; k < (int)indices.size(); ++k) {
            ans[indices[k]] = arr[start + k].first;
        }

        start = end;
    }

    return ans;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, limit;
    cin >> n >> limit;

    vector<int> nums(n);
    for (int i = 0; i < n; ++i) {
        cin >> nums[i];
    }

    vector<int> ans = solve(nums, limit);
    for (int i = 0; i < n; ++i) {
        if (i) cout << ' ';
        cout << ans[i];
    }
    cout << '\n';

    return 0;
}
```

### Python 3 ACM 模式

```python
import sys


def solve(nums, limit):
    n = len(nums)
    arr = sorted((num, i) for i, num in enumerate(nums))
    ans = [0] * n

    start = 0
    while start < n:
        end = start + 1

        while end < n and arr[end][0] - arr[end - 1][0] <= limit:
            end += 1

        indices = sorted(idx for _, idx in arr[start:end])
        values = [num for num, _ in arr[start:end]]

        for idx, value in zip(indices, values):
            ans[idx] = value

        start = end

    return ans


def main():
    data = list(map(int, sys.stdin.read().split()))
    if not data:
        return

    n, limit = data[0], data[1]
    nums = data[2:2 + n]

    print(*solve(nums, limit))


if __name__ == "__main__":
    main()
```

## 示例验证

```python
s = Solution()
assert s.lexicographicallySmallestArray([1, 5, 3, 9, 8], 2) == [1, 3, 5, 8, 9]
assert s.lexicographicallySmallestArray([1, 7, 6, 18, 2, 1], 3) == [1, 6, 7, 18, 1, 2]
assert s.lexicographicallySmallestArray([1, 7, 28, 19, 10], 3) == [1, 7, 28, 19, 10]
```

## 小结

这题的关键是把“能通过若干次交换到达”的元素看成同一个连通块。排序后用相邻差值判断分组，再把每组的小值放到最小下标上，就能得到全局字典序最小的数组。
