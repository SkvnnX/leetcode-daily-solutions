# 2026-08-30｜2091. 从数组中移除最大值和最小值

- 题号：2091
- 题名：从数组中移除最大值和最小值
- 英文题名：Removing Minimum and Maximum From Array
- 难度：中等 / Medium
- 标签：数组、贪心
- 链接：https://leetcode.cn/problems/removing-minimum-and-maximum-from-array/

## 题意概括

给定一个下标从 `0` 开始、元素互不相同的整数数组 `nums`。数组中有一个最小值和一个最大值。每次操作只能从数组的**前端**或**后端**删除一个元素，要求删除掉最小值和最大值，返回所需的最少删除次数。

## 关键思路

真正需要关心的只有两个位置：

- 最小值所在下标 `i`
- 最大值所在下标 `j`

假设 `i < j`，也就是左边那个目标位置是 `i`，右边那个目标位置是 `j`。

要删除这两个元素，只有三种本质方案：

1. **都从左边删**  
   删除到右边那个目标位置 `j` 为止，需要 `j + 1` 次。

2. **都从右边删**  
   删除到左边那个目标位置 `i` 为止，需要 `n - i` 次。

3. **一边从左删，一边从右删**  
   左边删到 `i`，右边删到 `j`，需要：
   
   `i + 1 + n - j`

答案就是这三种方案的最小值。

为什么这样枚举是完整的？因为删除操作只能发生在数组两端。要删除中间某个目标元素，要么从左端一路删到它，要么从右端一路删到它。两个目标元素组合起来后，就只会落入：左左、右右、左右 这三种情况。

## 复杂度分析

- 时间复杂度：`O(n)`  
  需要一次遍历找到最大值和最小值的位置。

- 空间复杂度：`O(1)`  
  只使用常数个变量。

## 容易踩坑点

1. **数组长度为 1 时**  
   此时唯一元素既是最大值也是最小值，公式依然成立，答案为 `1`。

2. **要先保证 `i < j`**  
   如果最大值位置在左、最小值位置在右，需要交换两个下标，方便统一套公式。

3. **第三种方案不要漏掉**  
   有些情况从两边同时删比只从一边删更优，例如最小值靠右、最大值靠左。

## Python 3 解法

```python
from typing import List


class Solution:
    def minimumDeletions(self, nums: List[int]) -> int:
        n = len(nums)

        min_idx = nums.index(min(nums))
        max_idx = nums.index(max(nums))

        # 统一让 i 是左边的目标位置，j 是右边的目标位置
        i, j = sorted((min_idx, max_idx))

        # 方案 1：两个目标都从左边删掉
        remove_from_left = j + 1

        # 方案 2：两个目标都从右边删掉
        remove_from_right = n - i

        # 方案 3：左边删一个目标，右边删一个目标
        remove_from_both_sides = (i + 1) + (n - j)

        return min(remove_from_left, remove_from_right, remove_from_both_sides)
```

## 示例验证

```python
s = Solution()
assert s.minimumDeletions([2, 10, 7, 5, 4, 1, 8, 6]) == 5
assert s.minimumDeletions([0, -4, 19, 1, 8, -2, -3, 5]) == 3
assert s.minimumDeletions([101]) == 1
```

## 小结

这题的核心不是模拟删除过程，而是把操作抽象成“从左删到某个位置”或“从右删到某个位置”。由于只有最大值和最小值两个目标点，枚举三种删除方向组合即可得到最优答案。
