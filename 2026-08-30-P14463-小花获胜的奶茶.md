# 2026-08-30｜P14463. 小花获胜的奶茶

- 题号：P14463
- 题名：小花获胜的奶茶
- 分值：100 分
- 难度：简单
- 标签：双指针、定长滑动窗口
- 来源：CodeFun2000 / 华为 OD
- 原题链接：https://codefun2000.com/new-p/P14463

## 题意概括

给定一个长度不超过 `10^6` 的整数数组和整数 `K`，从数组中选择连续 `K` 个数字，求它们的最大和。

- 数组元素可能是负数；
- `0 <= K <= n`；
- 当 `K == 0` 时，连续 `0` 个数的和定义为 `0`。

## 关键思路

这是标准的**定长滑动窗口**。

先计算前 `K` 个元素的和，作为第一个窗口。窗口每向右移动一格：

1. 加入新进入窗口的元素 `nums[right]`；
2. 减去离开窗口的元素 `nums[right - K]`；
3. 用当前窗口和更新最大值。

这样每次移动只做常数次运算，不必为每个起点重新计算 `K` 个数的和。

> 数组元素可达到 32 位整数边界，且 `n` 最大为 `10^6`，窗口和可能超过 `int`，C++ 必须使用 `long long`。

## 正确性说明

长度为 `K` 的连续区间共有 `n - K + 1` 个。算法先计算第一个区间的和；之后每次删除上一个区间最左元素、加入下一个区间最右元素，恰好得到下一个长度为 `K` 的区间和。因此算法不重不漏地检查了所有合法区间，取到的最大值就是答案。

## 复杂度分析

- 时间复杂度：`O(n)`；
- 空间复杂度：`O(1)`（不计输入数组）。

## 容易踩坑点

1. `K == 0` 时直接返回 `0`，不能继续访问第一个窗口。
2. 全是负数时不能把答案初始化为 `0`，应初始化为第一个窗口的和。
3. C++ 窗口和与答案使用 `long long`，防止溢出。
4. 这是固定长度窗口，不需要 `while` 收缩窗口。

## C++ 解法

```cpp
#include <algorithm>
#include <vector>
using namespace std;

class Solution {
public:
    long long maxFixedWindowSum(const vector<int>& nums, int k) {
        if (k == 0) return 0;

        long long windowSum = 0;
        for (int i = 0; i < k; ++i) {
            windowSum += nums[i];
        }

        long long answer = windowSum;
        for (int right = k; right < static_cast<int>(nums.size()); ++right) {
            windowSum += static_cast<long long>(nums[right]) - nums[right - k];
            answer = max(answer, windowSum);
        }
        return answer;
    }
};
```

## Python 3 解法

```python
from typing import List


class Solution:
    def max_fixed_window_sum(self, nums: List[int], k: int) -> int:
        if k == 0:
            return 0

        window_sum = sum(nums[:k])
        answer = window_sum
        for right in range(k, len(nums)):
            window_sum += nums[right] - nums[right - k]
            answer = max(answer, window_sum)
        return answer
```

## ACM 答题模式

输入为数组和 `K`，格式与原题一致：

```text
[1,3,8,6,4,10],3
```

输出：

```text
20
```

### C++ ACM 模式

```cpp
#include <algorithm>
#include <iostream>
#include <regex>
#include <string>
#include <vector>
using namespace std;

long long solve(const vector<long long>& nums, int k) {
    if (k == 0) return 0;

    long long windowSum = 0;
    for (int i = 0; i < k; ++i) windowSum += nums[i];

    long long answer = windowSum;
    for (int right = k; right < static_cast<int>(nums.size()); ++right) {
        windowSum += nums[right] - nums[right - k];
        answer = max(answer, windowSum);
    }
    return answer;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string input((istreambuf_iterator<char>(cin)), istreambuf_iterator<char>());
    regex numberPattern("-?\\d+");
    vector<long long> data;
    for (sregex_iterator it(input.begin(), input.end(), numberPattern), end;
         it != end; ++it) {
        data.push_back(stoll(it->str()));
    }

    int k = static_cast<int>(data.back());
    data.pop_back();
    cout << solve(data, k) << '\n';
    return 0;
}
```

### Python 3 ACM 模式

```python
import re
import sys


def solve(nums, k):
    if k == 0:
        return 0

    window_sum = sum(nums[:k])
    answer = window_sum
    for right in range(k, len(nums)):
        window_sum += nums[right] - nums[right - k]
        answer = max(answer, window_sum)
    return answer


def main():
    data = list(map(int, re.findall(r"-?\d+", sys.stdin.read())))
    k = data[-1]
    nums = data[:-1]
    print(solve(nums, k))


if __name__ == "__main__":
    main()
```

## 示例验证

- `[1,3,8,6,4,10],3`：最大窗口 `[6,4,10]`，输出 `20`；
- `[1,2,3,4,5,6,7,8,5,4,3,2,1],4`：输出 `26`；
- `[1,2,3],0`：输出 `0`；
- `[-8,-3,-5],2`：输出 `-8`，验证全负数场景。

## 小结

看到“**连续、长度固定、求最大/最小和**”，优先想到定长滑动窗口：首个窗口完整求和，之后每次“一进一出”。
