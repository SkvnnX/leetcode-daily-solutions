# 2026-08-30｜P14464. 快递驿站计费系统

- 题号：P14464
- 题名：快递驿站计费系统
- 分值：100 分
- 难度：简单
- 标签：模拟、哈希表、排序
- 来源：CodeFun2000 / 华为 OD
- 原题链接：https://codefun2000.com/new-p/P14464

## 题意概括

每条快递记录包含 `[存件时间, 取件时间, 房间号]`。需要计算每位业主的快递总费用并排序输出。

计费规则：

1. 存放时间 `duration = 取件时间 - 存件时间`；
2. `duration <= 12` 小时免费；
3. 超过 12 小时后，每 12 小时收费 1 元，不足 12 小时按 12 小时计算；
4. 同一房间的多条记录需要累加；
5. 按总费用降序输出，费用相同按房间号升序输出；
6. 出现过但费用为 0 的房间也必须输出。

## 关键思路

这是一道“**逐条模拟 + 按键聚合 + 自定义排序**”题。

对每条记录：

```text
overtime = max(0, duration - 12)
fee = ceil(overtime / 12)
```

整数运算中，非负整数向上取整可写成：

```text
fee = (overtime + 11) / 12
```

用哈希表 `totalFee[room]` 累加每个房间的费用。遍历完成后，将哈希表转成二维列表，再以 `(-总费用, 房间号)` 为关键字排序。

## 正确性说明

每条快递记录的费用只由其存放时长决定，公式准确实现了免费 12 小时及超出部分按 12 小时向上取整的规则。哈希表把同一房间所有记录的费用相加，因此得到每位业主的总费用。最后按“费用降序、房间号升序”排序，恰好符合输出要求。

## 复杂度分析

设记录数为 `N`，不同房间数为 `R`：

- 时间复杂度：`O(N + R log R)`；
- 空间复杂度：`O(R)`。

## 容易踩坑点

1. 先扣除免费的 12 小时，再对超时部分向上取整。
2. 存放 12 小时收费 `0`；13 小时收费 `1`；24 小时仍收费 `1`；25 小时收费 `2`。
3. 同一房间要累计所有订单，不能覆盖旧费用。
4. 零费用房间也要保留在结果中。
5. 排序有两个关键字：先费用降序，再房间号升序。

## C++ 解法

```cpp
#include <algorithm>
#include <unordered_map>
#include <utility>
#include <vector>
using namespace std;

class Solution {
public:
    vector<vector<long long>> calculateFees(const vector<vector<int>>& records) {
        unordered_map<int, long long> totalFee;

        for (const auto& record : records) {
            long long duration = static_cast<long long>(record[1]) - record[0];
            long long overtime = max(0LL, duration - 12);
            long long fee = (overtime + 11) / 12;
            totalFee[record[2]] += fee;
        }

        vector<pair<int, long long>> owners(totalFee.begin(), totalFee.end());
        sort(owners.begin(), owners.end(), [](const auto& a, const auto& b) {
            if (a.second != b.second) return a.second > b.second;
            return a.first < b.first;
        });

        vector<vector<long long>> answer;
        for (const auto& [room, fee] : owners) {
            answer.push_back({room, fee});
        }
        return answer;
    }
};
```

## Python 3 解法

```python
from collections import defaultdict
from typing import List


class Solution:
    def calculate_fees(self, records: List[List[int]]) -> List[List[int]]:
        total_fee = defaultdict(int)

        for stored_at, picked_up_at, room in records:
            duration = picked_up_at - stored_at
            overtime = max(0, duration - 12)
            fee = (overtime + 11) // 12
            total_fee[room] += fee

        return [
            [room, fee]
            for room, fee in sorted(
                total_fee.items(), key=lambda item: (-item[1], item[0])
            )
        ]
```

## ACM 答题模式

输入为二维记录数组：

```text
[[100, 110, 1111], [200, 210, 2222], [150, 165, 3333], [180, 190, 1111]]
```

输出：

```text
[[3333,1],[1111,0],[2222,0]]
```

### C++ ACM 模式

```cpp
#include <algorithm>
#include <iostream>
#include <regex>
#include <string>
#include <unordered_map>
#include <utility>
#include <vector>
using namespace std;

vector<pair<int, long long>> solve(const vector<vector<int>>& records) {
    unordered_map<int, long long> totalFee;
    for (const auto& record : records) {
        long long duration = static_cast<long long>(record[1]) - record[0];
        long long overtime = max(0LL, duration - 12);
        totalFee[record[2]] += (overtime + 11) / 12;
    }

    vector<pair<int, long long>> answer(totalFee.begin(), totalFee.end());
    sort(answer.begin(), answer.end(), [](const auto& a, const auto& b) {
        if (a.second != b.second) return a.second > b.second;
        return a.first < b.first;
    });
    return answer;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string input((istreambuf_iterator<char>(cin)), istreambuf_iterator<char>());
    regex numberPattern("\\d+");
    vector<int> data;
    for (sregex_iterator it(input.begin(), input.end(), numberPattern), end;
         it != end; ++it) {
        data.push_back(stoi(it->str()));
    }

    vector<vector<int>> records;
    for (int i = 0; i + 2 < static_cast<int>(data.size()); i += 3) {
        records.push_back({data[i], data[i + 1], data[i + 2]});
    }

    auto answer = solve(records);
    cout << '[';
    for (int i = 0; i < static_cast<int>(answer.size()); ++i) {
        if (i > 0) cout << ',';
        cout << '[' << answer[i].first << ',' << answer[i].second << ']';
    }
    cout << "]\n";
    return 0;
}
```

### Python 3 ACM 模式

```python
import ast
import sys
from collections import defaultdict


def solve(records):
    total_fee = defaultdict(int)
    for stored_at, picked_up_at, room in records:
        overtime = max(0, picked_up_at - stored_at - 12)
        total_fee[room] += (overtime + 11) // 12

    return [
        [room, fee]
        for room, fee in sorted(
            total_fee.items(), key=lambda item: (-item[1], item[0])
        )
    ]


def main():
    records = ast.literal_eval(sys.stdin.read().strip())
    answer = solve(records)
    print(str(answer).replace(" ", ""))


if __name__ == "__main__":
    main()
```

## 示例验证

- 样例 1 输出 `[[3333,1],[1111,0],[2222,0]]`；
- 样例 2 输出 `[[102,4],[201,4],[303,3]]`；
- 边界时长 `12、13、24、25` 小时对应费用 `0、1、1、2`。

## 小结

先把自然语言规则翻译成单条记录的整数计费公式，再用哈希表聚合，最后按多关键字排序。此类题的重点通常不在算法难度，而在边界和排序规则是否完全落实。
