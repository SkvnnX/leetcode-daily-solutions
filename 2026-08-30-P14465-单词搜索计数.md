# 2026-08-30｜P14465. 单词搜索计数

- 题号：P14465
- 题名：单词搜索计数
- 分值：200 分
- 难度：中等
- 标签：DFS、回溯、网格搜索
- 来源：CodeFun2000 / 华为 OD
- 原题链接：https://codefun2000.com/new-p/P14465

## 题意概括

给定一个 `m × n` 的大写字母网格 `board` 和字符串 `word`，统计 `word` 在网格中出现的**路径总数**。

路径规则：

1. 每一步只能走到上下左右相邻格子，不能走对角线；
2. 路径上的字符顺序必须与 `word` 一致；
3. 同一个格子在同一条路径中不能重复使用；
4. 要统计所有合法路径，而不是只判断是否存在。

约束：`1 <= m,n <= 10`，`1 <= len(word) <= 100`。

## 关键思路

枚举网格中的每个格子作为起点，并从起点执行 DFS / 回溯。

定义 `dfs(r, c, index)`：当前位于 `(r,c)`，准备匹配 `word[index]`，返回从这里出发能够构成后续单词的路径数量。

递归过程：

1. 当前字符不等于 `word[index]`，返回 `0`；
2. 已匹配到最后一个字符，返回 `1`；
3. 临时把当前格子标记为已使用；
4. 向上下左右递归，累加各方向返回的路径数；
5. 恢复当前格子，使它还能参与其他搜索路径。

最后将所有起点的 DFS 返回值相加。

### 为什么必须回溯恢复

“不能重复使用格子”只限制**当前路径**。一条路径搜索结束后，其他起点或其他分支仍然可以使用这个格子。如果只标记不恢复，会错误地漏掉合法路径。

## 正确性说明

对任一起点，DFS 只沿上下左右扩展，并在当前路径中标记已用格子，因此生成的每条路径都满足相邻规则和不可重复使用规则。递归仅在当前字符匹配 `word[index]` 时继续，到达末字符时恰好计数一次，所以每条被计数路径都合法。

反过来，任意合法路径都有唯一的起点；DFS 从该起点会依次尝试路径上的每个相邻格子，并因字符匹配而走完整条路径，最终计数一次。因此所有合法路径都会被统计，且不会重复或遗漏。

## 复杂度分析

令 `L = len(word)`：

- 时间复杂度：最坏为 `O(mn × 3^(L-1))`。首步最多有 4 个方向，之后由于当前路径不能立即回到上一个格子，可用 `3` 作为常见上界；
- 空间复杂度：`O(L)`，来自递归栈；网格原地标记，不需要额外 `m × n` 访问数组；
- 当 `L > mn` 时不可能成功，可直接返回 `0`。

## 容易踩坑点

1. 题目要求统计路径数，找到一条后不能提前返回。
2. 只允许四方向，不包含四个对角方向。
3. 已访问标记必须是“当前路径级别”，递归返回后必须恢复。
4. 不同方向或不同起点得到的路径都应分别计数。
5. `word` 长度大于格子总数时直接返回 `0`，避免无意义搜索。

## C++ 解法

```cpp
#include <string>
#include <vector>
using namespace std;

class Solution {
    int rows;
    int cols;
    string word;

    long long dfs(vector<vector<char>>& board, int row, int col, int index) {
        if (board[row][col] != word[index]) return 0;
        if (index == static_cast<int>(word.size()) - 1) return 1;

        char original = board[row][col];
        board[row][col] = '#';

        long long paths = 0;
        const int directions[4][2] = {{-1, 0}, {1, 0}, {0, -1}, {0, 1}};
        for (const auto& direction : directions) {
            int nextRow = row + direction[0];
            int nextCol = col + direction[1];
            if (nextRow < 0 || nextRow >= rows || nextCol < 0 || nextCol >= cols) {
                continue;
            }
            paths += dfs(board, nextRow, nextCol, index + 1);
        }

        board[row][col] = original;
        return paths;
    }

public:
    long long countWordPaths(vector<vector<char>>& board, const string& target) {
        rows = board.size();
        cols = board[0].size();
        word = target;

        if (word.size() > static_cast<size_t>(rows * cols)) return 0;

        long long answer = 0;
        for (int row = 0; row < rows; ++row) {
            for (int col = 0; col < cols; ++col) {
                answer += dfs(board, row, col, 0);
            }
        }
        return answer;
    }
};
```

## Python 3 解法

```python
from typing import List


class Solution:
    def count_word_paths(self, board: List[List[str]], word: str) -> int:
        rows, cols = len(board), len(board[0])
        if len(word) > rows * cols:
            return 0

        def dfs(row: int, col: int, index: int) -> int:
            if board[row][col] != word[index]:
                return 0
            if index == len(word) - 1:
                return 1

            original = board[row][col]
            board[row][col] = "#"
            paths = 0

            for dr, dc in ((-1, 0), (1, 0), (0, -1), (0, 1)):
                next_row, next_col = row + dr, col + dc
                if 0 <= next_row < rows and 0 <= next_col < cols:
                    paths += dfs(next_row, next_col, index + 1)

            board[row][col] = original
            return paths

        return sum(
            dfs(row, col, 0)
            for row in range(rows)
            for col in range(cols)
        )
```

## ACM 答题模式

输入格式与原题一致：二维字符数组后跟目标单词。

```text
[["A","B","C","E"],["S","F","C","S"],["A","D","E","E"]],"ABCCED"
```

输出：

```text
1
```

### C++ ACM 模式

```cpp
#include <iostream>
#include <string>
#include <vector>
using namespace std;

long long dfs(vector<vector<char>>& board, const string& word,
              int row, int col, int index) {
    int rows = board.size();
    int cols = board[0].size();
    if (board[row][col] != word[index]) return 0;
    if (index == static_cast<int>(word.size()) - 1) return 1;

    char original = board[row][col];
    board[row][col] = '#';

    long long paths = 0;
    const int directions[4][2] = {{-1, 0}, {1, 0}, {0, -1}, {0, 1}};
    for (const auto& direction : directions) {
        int nextRow = row + direction[0];
        int nextCol = col + direction[1];
        if (nextRow < 0 || nextRow >= rows || nextCol < 0 || nextCol >= cols) {
            continue;
        }
        paths += dfs(board, word, nextRow, nextCol, index + 1);
    }

    board[row][col] = original;
    return paths;
}

long long solve(vector<vector<char>>& board, const string& word) {
    int rows = board.size();
    int cols = board[0].size();
    if (word.size() > static_cast<size_t>(rows * cols)) return 0;

    long long answer = 0;
    for (int row = 0; row < rows; ++row) {
        for (int col = 0; col < cols; ++col) {
            answer += dfs(board, word, row, col, 0);
        }
    }
    return answer;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string input((istreambuf_iterator<char>(cin)), istreambuf_iterator<char>());
    vector<vector<char>> board;
    vector<char> currentRow;
    int depth = 0;
    size_t boardEnd = 0;

    for (size_t i = 0; i < input.size(); ++i) {
        if (input[i] == '[') {
            ++depth;
        } else if (input[i] == ']') {
            if (depth == 2) {
                board.push_back(currentRow);
                currentRow.clear();
            }
            --depth;
            if (depth == 0) {
                boardEnd = i;
                break;
            }
        } else if (input[i] == '"' && depth == 2) {
            size_t closingQuote = input.find('"', i + 1);
            currentRow.push_back(input[i + 1]);
            i = closingQuote;
        }
    }

    size_t wordStart = input.find('"', boardEnd + 1);
    size_t wordEnd = input.find('"', wordStart + 1);
    string word = input.substr(wordStart + 1, wordEnd - wordStart - 1);

    cout << solve(board, word) << '\n';
    return 0;
}
```

### Python 3 ACM 模式

```python
import ast
import sys


def solve(board, word):
    rows, cols = len(board), len(board[0])
    if len(word) > rows * cols:
        return 0

    def dfs(row, col, index):
        if board[row][col] != word[index]:
            return 0
        if index == len(word) - 1:
            return 1

        original = board[row][col]
        board[row][col] = "#"
        paths = 0
        for dr, dc in ((-1, 0), (1, 0), (0, -1), (0, 1)):
            next_row, next_col = row + dr, col + dc
            if 0 <= next_row < rows and 0 <= next_col < cols:
                paths += dfs(next_row, next_col, index + 1)
        board[row][col] = original
        return paths

    return sum(
        dfs(row, col, 0)
        for row in range(rows)
        for col in range(cols)
    )


def main():
    board, word = ast.literal_eval("[" + sys.stdin.read().strip() + "]")
    print(solve(board, word))


if __name__ == "__main__":
    main()
```

## 示例验证

1. `[["A","B","C","E"],["S","F","C","S"],["A","D","E","E"]],"ABCCED"` 输出 `1`；
2. `[["A","A"]],"A"` 输出 `2`；
3. `[["A","B"],["C","D"]],"ABCD"` 输出 `0`；
4. `[["A","A"],["A","A"]],"AA"` 输出 `8`，验证不同起点和方向都分别计数；
5. `[["A","B"]],"ABA"` 输出 `0`，验证同一格不能在一条路径中重复使用。

## 小结

这题与经典“单词搜索”最大的区别是：经典题通常只判断存在性，本题需要**累加全部路径数**。DFS 返回值应表示“从当前状态出发的合法路径数量”，各方向结果相加，并在每次递归后恢复现场。
