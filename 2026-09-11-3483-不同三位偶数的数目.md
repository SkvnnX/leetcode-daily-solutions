# 2026-09-11 LeetCode 每日一题：3483. 不同三位偶数的数目

## 题目信息

- 日期：2026-09-11
- 题号：3483
- 题名：不同三位偶数的数目（Unique 3-Digit Even Numbers）
- 难度：简单
- 标签/考点：数组、哈希表、枚举、计数、递归/回溯思想
- 题目链接：https://leetcode.cn/problems/unique-3-digit-even-numbers/
- 官方来源：力扣 GraphQL `todayRecord`，日期 `2026-09-11`

## 题意概括

给定一个数字数组 `digits`，每个元素是 `0` 到 `9` 的一位数字。要求从数组中选出三个数字，组成一个**没有前导零**的三位偶数。数组中每个数字副本在一个数里最多只能使用一次。返回可以组成的**不同**三位偶数的数量。

例如 `digits = [0,2,2]` 可以组成 `202` 和 `220`，答案是 `2`。

## 关键思路

本题数据范围很小：`digits.length <= 10`，数字只可能是 `0..9`。与其枚举下标排列后再去重，不如直接枚举所有三位偶数：

1. 三位数范围是 `100..999`，偶数只需要枚举 `100, 102, ..., 998`。
2. 对每个候选数拆成百位、十位、个位。
3. 统计候选数需要的各数字次数，例如 `220` 需要两个 `2` 和一个 `0`。
4. 只要候选数中每个数字的需要次数都不超过 `digits` 中的出现次数，它就可以被组成。
5. 每个候选数只枚举一次，因此天然保证“不同数字”不会重复计数。

### 为什么这样做是正确的？

任何合法答案一定是一个三位偶数，所以它必然出现在 `100..998` 的偶数枚举中；如果它能由 `digits` 组成，那么它的每个数字出现次数不会超过数组提供的次数，因此会被计入。反过来，如果某个候选数通过了次数检查，它是三位数、个位为偶数、无前导零，并且没有超量使用任何数字，所以它一定是一个合法答案。两边互相覆盖，因此计数正确。

## 复杂度分析

设 `U = 450`，即三位偶数的个数。

- 时间复杂度：`O(n + U * 10)`，其中 `n` 是 `digits.length`。由于 `U` 和数字种类 `10` 都是常数，也可视为 `O(n)`。
- 空间复杂度：`O(10)`，只需要两个长度为 10 的计数数组。

## 容易踩坑点

1. **不能有前导零**：直接枚举 `100..998` 可以避免这个问题；如果枚举三位数字，要特别排除百位为 `0`。
2. **要统计不同数字而不是方案数**：例如同一个数字可能由不同下标副本组成，但只算一次。枚举具体数值可以天然去重。
3. **重复数字需要检查次数**：`digits = [0,2,2]` 可以组成 `220`，但 `digits = [0,2]` 不行。
4. **个位必须是偶数**：只枚举偶数，或手动检查个位 `0,2,4,6,8`。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    int totalNumbers(vector<int>& digits) {
        vector<int> have(10, 0);
        for (int d : digits) {
            ++have[d];
        }

        int ans = 0;
        for (int x = 100; x <= 998; x += 2) {
            vector<int> need(10, 0);
            int t = x;
            ++need[t % 10];
            t /= 10;
            ++need[t % 10];
            t /= 10;
            ++need[t % 10];

            bool ok = true;
            for (int d = 0; d < 10; ++d) {
                if (need[d] > have[d]) {
                    ok = false;
                    break;
                }
            }
            if (ok) {
                ++ans;
            }
        }
        return ans;
    }
};
```

## LeetCode 可提交解法：Python 3

```python
from typing import List

class Solution:
    def totalNumbers(self, digits: List[int]) -> int:
        have = [0] * 10
        for d in digits:
            have[d] += 1

        ans = 0
        for x in range(100, 999, 2):
            need = [0] * 10
            t = x
            need[t % 10] += 1
            t //= 10
            need[t % 10] += 1
            t //= 10
            need[t % 10] += 1

            if all(need[d] <= have[d] for d in range(10)):
                ans += 1
        return ans
```

## ACM 答题模式

### 自定义输入格式

第一行输入整数 `n`，表示数组长度。第二行输入 `n` 个整数，表示 `digits`。

```text
n
d1 d2 ... dn
```

输出一个整数，表示能组成的不同三位偶数数量。

### ACM 模式 C++ 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int totalNumbers(const vector<int>& digits) {
    vector<int> have(10, 0);
    for (int d : digits) {
        ++have[d];
    }

    int ans = 0;
    for (int x = 100; x <= 998; x += 2) {
        vector<int> need(10, 0);
        int t = x;
        ++need[t % 10];
        t /= 10;
        ++need[t % 10];
        t /= 10;
        ++need[t % 10];

        bool ok = true;
        for (int d = 0; d < 10; ++d) {
            if (need[d] > have[d]) {
                ok = false;
                break;
            }
        }
        if (ok) {
            ++ans;
        }
    }
    return ans;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    vector<int> digits(n);
    for (int i = 0; i < n; ++i) {
        cin >> digits[i];
    }

    cout << totalNumbers(digits) << '\n';
    return 0;
}
```

### ACM 模式 Python 3 完整代码

```python
import sys


def total_numbers(digits):
    have = [0] * 10
    for d in digits:
        have[d] += 1

    ans = 0
    for x in range(100, 999, 2):
        need = [0] * 10
        t = x
        need[t % 10] += 1
        t //= 10
        need[t % 10] += 1
        t //= 10
        need[t % 10] += 1

        if all(need[d] <= have[d] for d in range(10)):
            ans += 1
    return ans


def main():
    data = list(map(int, sys.stdin.read().split()))
    if not data:
        return
    n = data[0]
    digits = data[1:1 + n]
    print(total_numbers(digits))


if __name__ == "__main__":
    main()
```

## 示例与边界验证

| 输入 digits | 期望输出 | 说明 |
|---|---:|---|
| `[1,2,3,4]` | `12` | 官方示例 1 |
| `[0,2,2]` | `2` | 官方示例 2，重复数字可按副本使用 |
| `[6,6,6]` | `1` | 官方示例 3 |
| `[1,3,5]` | `0` | 官方示例 4，没有偶数个位 |
| `[0,0,0]` | `0` | 边界：不能组成有前导零的三位数 |
| `[0,1,2]` | `3` | 可组成 `102`、`120`、`210` |
