# 2026-09-12 LeetCode 每日一题：3414. 不重叠区间的最大得分

- **日期**：2026-09-12
- **题号**：3414
- **题名**：不重叠区间的最大得分
- **难度**：困难
- **标签 / 考点**：数组、二分查找、动态规划、排序
- **题目链接**：[https://leetcode.cn/problems/maximum-score-of-non-overlapping-intervals/](https://leetcode.cn/problems/maximum-score-of-non-overlapping-intervals/)

## 题意概括

给定若干区间 `intervals[i] = [l_i, r_i, weight_i]`，最多选择 4 个两两不重叠的区间，使权重和最大。两个区间只要有公共点就算重叠，因此前一个区间右端点为 `r` 时，下一个区间必须满足 `l > r`。若最大得分方案不唯一，返回字典序最小的原始下标数组。

## 关键思路

按左端点排序区间，并保留原始下标。对每个区间 `[l, r, w]`，用二分找到第一个左端点大于 `r` 的区间位置 `nxt`。定义 `dp[k][i]` 表示只看排序后 `i..n-1` 的区间、最多还能选 `k` 个区间时的最优方案。

转移分两类：不选当前区间，继承 `dp[k][i+1]`；选当前区间，得到 `w + dp[k-1][nxt]`。比较方案时先比较总权重，权重相同再比较原始下标数组的字典序。因为最多选 4 个区间，每个状态保存的下标数组很短，直接复制并排序即可。

## 为什么这样做

排序后，如果当前区间被选中，所有与它不重叠且可能在后面继续选的区间，一定从第一个 `start > r` 的位置开始；边界相等仍重叠，所以这里要用 `upper_bound / bisect_right`。这样“当前区间选不选”的后续子问题范围是确定的，满足动态规划的最优子结构。每一步都保留同分时字典序更小的方案，最终 `dp[4][0]` 就是全局最大得分且字典序最小的答案。

## 复杂度分析

- 时间复杂度：排序 `O(n log n)`；DP 有 `4n` 个状态，每个状态一次二分，整体 `O(n log n)`。
- 空间复杂度：`O(4n)`，每个状态最多保存 4 个下标。

## 容易踩坑点

- 下一个区间必须满足 `start > end`，不能把边界相等的区间当作不重叠。
- 答案返回原始下标，不是排序后的位置。
- 权重相同要比较下标数组字典序，不能随意保留先遇到的方案。
- “最多选 4 个”不是“必须选 4 个”。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    vector<int> maximumWeight(vector<vector<int>>& intervals) {
        int n = intervals.size();
        vector<array<long long, 4>> a(n);
        for (int i = 0; i < n; ++i) a[i] = {intervals[i][0], intervals[i][1], intervals[i][2], i};
        sort(a.begin(), a.end(), [](auto &x, auto &y){ return x[0] == y[0] ? x[1] < y[1] : x[0] < y[0]; });
        vector<long long> starts(n);
        for (int i = 0; i < n; ++i) starts[i] = a[i][0];
        vector<vector<long long>> score(5, vector<long long>(n + 1));
        vector<vector<vector<int>>> ids(5, vector<vector<int>>(n + 1));
        auto better = [](long long s1, const vector<int>& v1, long long s2, const vector<int>& v2) {
            if (s1 != s2) return s1 > s2;
            return lexicographical_compare(v1.begin(), v1.end(), v2.begin(), v2.end());
        };
        for (int i = n - 1; i >= 0; --i) {
            int nxt = upper_bound(starts.begin(), starts.end(), a[i][1]) - starts.begin();
            for (int k = 1; k <= 4; ++k) {
                long long ts = a[i][2] + score[k - 1][nxt];
                vector<int> ti = ids[k - 1][nxt];
                ti.push_back((int)a[i][3]);
                sort(ti.begin(), ti.end());
                long long ss = score[k][i + 1];
                const vector<int>& si = ids[k][i + 1];
                if (better(ts, ti, ss, si)) score[k][i] = ts, ids[k][i] = move(ti);
                else score[k][i] = ss, ids[k][i] = si;
            }
        }
        return ids[4][0];
    }
};
```

## LeetCode 可提交解法：Python 3

```python
class Solution:
    def maximumWeight(self, intervals: List[List[int]]) -> List[int]:
        from bisect import bisect_right
        arr = sorted((l, r, w, i) for i, (l, r, w) in enumerate(intervals))
        n = len(arr)
        starts = [x[0] for x in arr]
        score = [[0] * (n + 1) for _ in range(5)]
        ids = [[() for _ in range(n + 1)] for _ in range(5)]
        for i in range(n - 1, -1, -1):
            l, r, w, idx = arr[i]
            nxt = bisect_right(starts, r)  # 必须 start > r，边界相等算重叠
            for k in range(1, 5):
                take_score = w + score[k - 1][nxt]
                take_ids = tuple(sorted((idx,) + ids[k - 1][nxt]))
                skip_score = score[k][i + 1]
                skip_ids = ids[k][i + 1]
                if take_score > skip_score or (take_score == skip_score and take_ids < skip_ids):
                    score[k][i] = take_score
                    ids[k][i] = take_ids
                else:
                    score[k][i] = skip_score
                    ids[k][i] = skip_ids
        return list(ids[4][0])
```

## ACM 答题模式

### 自定义输入格式

第一行输入整数 `n`。接下来 `n` 行，每行输入 `l r weight`。输出被选中的原始下标，空格分隔。

```text
6
1 3 2
4 5 2
1 5 5
6 9 3
6 7 1
8 9 1
```

```text
2 3
```

### ACM C++ 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

vector<int> maximumWeight(vector<vector<int>>& intervals) {
    int n = intervals.size();
    vector<array<long long, 4>> a(n);
    for (int i = 0; i < n; ++i) a[i] = {intervals[i][0], intervals[i][1], intervals[i][2], i};
    sort(a.begin(), a.end(), [](auto &x, auto &y){ return x[0] == y[0] ? x[1] < y[1] : x[0] < y[0]; });
    vector<long long> starts(n);
    for (int i = 0; i < n; ++i) starts[i] = a[i][0];
    vector<vector<long long>> score(5, vector<long long>(n + 1));
    vector<vector<vector<int>>> ids(5, vector<vector<int>>(n + 1));
    auto better = [](long long s1, const vector<int>& v1, long long s2, const vector<int>& v2) {
        if (s1 != s2) return s1 > s2;
        return lexicographical_compare(v1.begin(), v1.end(), v2.begin(), v2.end());
    };
    for (int i = n - 1; i >= 0; --i) {
        int nxt = upper_bound(starts.begin(), starts.end(), a[i][1]) - starts.begin();
        for (int k = 1; k <= 4; ++k) {
            long long ts = a[i][2] + score[k - 1][nxt];
            vector<int> ti = ids[k - 1][nxt];
            ti.push_back((int)a[i][3]);
            sort(ti.begin(), ti.end());
            long long ss = score[k][i + 1];
            const vector<int>& si = ids[k][i + 1];
            if (better(ts, ti, ss, si)) score[k][i] = ts, ids[k][i] = move(ti);
            else score[k][i] = ss, ids[k][i] = si;
        }
    }
    return ids[4][0];
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin >> n;
    vector<vector<int>> intervals(n, vector<int>(3));
    for (int i = 0; i < n; ++i) cin >> intervals[i][0] >> intervals[i][1] >> intervals[i][2];
    vector<int> ans = maximumWeight(intervals);
    for (int i = 0; i < (int)ans.size(); ++i) {
        if (i) cout << ' ';
        cout << ans[i];
    }
    cout << '
';
    return 0;
}
```

### ACM Python 3 完整代码

```python
from bisect import bisect_right
import sys

def maximum_weight(intervals):
    arr = sorted((l, r, w, i) for i, (l, r, w) in enumerate(intervals))
    n = len(arr)
    starts = [x[0] for x in arr]
    score = [[0] * (n + 1) for _ in range(5)]
    ids = [[() for _ in range(n + 1)] for _ in range(5)]
    for i in range(n - 1, -1, -1):
        l, r, w, idx = arr[i]
        nxt = bisect_right(starts, r)
        for k in range(1, 5):
            take_score = w + score[k - 1][nxt]
            take_ids = tuple(sorted((idx,) + ids[k - 1][nxt]))
            skip_score = score[k][i + 1]
            skip_ids = ids[k][i + 1]
            if take_score > skip_score or (take_score == skip_score and take_ids < skip_ids):
                score[k][i] = take_score
                ids[k][i] = take_ids
            else:
                score[k][i] = skip_score
                ids[k][i] = skip_ids
    return list(ids[4][0])

def main():
    data = list(map(int, sys.stdin.read().split()))
    if not data:
        return
    n = data[0]
    intervals = []
    p = 1
    for _ in range(n):
        intervals.append(data[p:p + 3])
        p += 3
    print(' '.join(map(str, maximum_weight(intervals))))

if __name__ == '__main__':
    main()
```
