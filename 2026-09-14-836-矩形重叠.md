# 2026-09-14 LeetCode 每日一题：836. 矩形重叠

- **日期**：2026-09-14
- **题号**：836
- **题名**：矩形重叠（Rectangle Overlap）
- **难度**：简单
- **标签 / 考点**：几何、数学、区间相交、边界判断
- **题目链接**：[https://leetcode.cn/problems/rectangle-overlap/](https://leetcode.cn/problems/rectangle-overlap/)
- **官方来源**：力扣 GraphQL `todayRecord`，日期 `2026-09-14`

## 题意概括

给定两个与坐标轴平行的矩形 `rec1 = [x1, y1, x2, y2]` 和 `rec2 = [x1, y1, x2, y2]`，其中 `(x1, y1)` 是左下角，`(x2, y2)` 是右上角。判断两个矩形是否有**正面积**重叠。

注意：如果两个矩形只是边或角接触，交集面积为 `0`，不算重叠。

## 关键思路

把二维矩形重叠拆成两个一维区间重叠：

1. 在 x 轴方向，两个投影区间必须有正长度交集；
2. 在 y 轴方向，两个投影区间也必须有正长度交集。

对于两个一维区间 `[l1, r1]` 和 `[l2, r2]`，它们有正长度交集，当且仅当：

```text
max(l1, l2) < min(r1, r2)
```

这里必须使用严格小于 `<`，因为相等表示只在端点接触，长度为 `0`。

因此答案是：

```text
max(rec1[0], rec2[0]) < min(rec1[2], rec2[2])
并且
max(rec1[1], rec2[1]) < min(rec1[3], rec2[3])
```

## 为什么这样做

矩形的交集仍然是一个与坐标轴平行的矩形或退化图形。它的横向宽度为：

```text
min(两个右边界) - max(两个左边界)
```

纵向高度为：

```text
min(两个上边界) - max(两个下边界)
```

只有当宽度和高度都大于 `0` 时，交集面积才是正数。任意一个方向没有正长度交集，交集面积就是 `0`，不满足题意。

## 复杂度分析

- **时间复杂度**：`O(1)`，只做常数次比较。
- **空间复杂度**：`O(1)`，不使用额外数据结构。

## 容易踩坑点

1. **边界相贴不算重叠**：例如 `[0,0,1,1]` 和 `[1,0,2,1]`，x 方向交集长度为 `0`，答案是 `false`。
2. **不要用 `<=` 判断正交集**：应使用 `<` 判断左边界最大值是否严格小于右边界最小值。
3. **坐标可能很大**：本题只比较大小，不需要计算面积，避免潜在溢出问题。
4. **两个方向都要满足**：只在 x 方向或只在 y 方向重叠都不够。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    bool isRectangleOverlap(vector<int>& rec1, vector<int>& rec2) {
        return max(rec1[0], rec2[0]) < min(rec1[2], rec2[2]) &&
               max(rec1[1], rec2[1]) < min(rec1[3], rec2[3]);
    }
};
```

## LeetCode 可提交解法：Python 3

```python
class Solution:
    def isRectangleOverlap(self, rec1: List[int], rec2: List[int]) -> bool:
        return max(rec1[0], rec2[0]) < min(rec1[2], rec2[2]) and \
               max(rec1[1], rec2[1]) < min(rec1[3], rec2[3])
```

## ACM 答题模式

### 自定义输入格式

输入共两行，每行 4 个整数：

```text
rec1_x1 rec1_y1 rec1_x2 rec1_y2
rec2_x1 rec2_y1 rec2_x2 rec2_y2
```

输出一行：如果两个矩形有正面积重叠，输出 `true`；否则输出 `false`。

示例输入：

```text
0 0 2 2
1 1 3 3
```

示例输出：

```text
true
```

### ACM 模式 C++

```cpp
#include <bits/stdc++.h>
using namespace std;

bool isRectangleOverlap(const vector<long long>& rec1, const vector<long long>& rec2) {
    return max(rec1[0], rec2[0]) < min(rec1[2], rec2[2]) &&
           max(rec1[1], rec2[1]) < min(rec1[3], rec2[3]);
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    vector<long long> rec1(4), rec2(4);
    for (int i = 0; i < 4; ++i) cin >> rec1[i];
    for (int i = 0; i < 4; ++i) cin >> rec2[i];

    cout << (isRectangleOverlap(rec1, rec2) ? "true" : "false") << '\n';
    return 0;
}
```

### ACM 模式 Python 3

```python
import sys


def is_rectangle_overlap(rec1, rec2):
    return max(rec1[0], rec2[0]) < min(rec1[2], rec2[2]) and \
           max(rec1[1], rec2[1]) < min(rec1[3], rec2[3])


def main():
    nums = list(map(int, sys.stdin.read().split()))
    if len(nums) != 8:
        raise ValueError("需要输入 8 个整数：rec1 的 4 个坐标和 rec2 的 4 个坐标")
    rec1 = nums[:4]
    rec2 = nums[4:]
    print("true" if is_rectangle_overlap(rec1, rec2) else "false")


if __name__ == "__main__":
    main()
```
