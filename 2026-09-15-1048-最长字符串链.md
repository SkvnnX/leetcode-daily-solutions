# 2026-09-15｜LeetCode 1048. 最长字符串链

> **题目来源标记：华为面试原题**

## 基本信息

- 题号：1048
- 题名：最长字符串链
- 英文题名：Longest String Chain
- 难度：中等
- 标签/考点：数组、哈希表、字符串、动态规划、排序
- 题目链接：https://leetcode.cn/problems/longest-string-chain/

## 题意概括

给定一个由小写英文字母组成的单词数组 `words`。

如果可以在单词 `A` 的任意位置插入恰好一个字母，并且不改变其他字符的相对顺序，从而得到单词 `B`，那么 `A` 是 `B` 的前身。

要求从数组中选出一条最长字符串链，使链中每个单词都是下一个单词的前身，返回链的最大长度。

## 关键思路：按长度排序 + 动态规划

前身一定比当前单词少一个字符。因此先按字符串长度从小到大排序，处理当前单词时，它所有可能的前身都已经处理完毕。

令 `dp[word]` 表示以 `word` 结尾的最长字符串链长度，初始值为 `1`。

对于当前单词 `word`，依次删除每个位置的一个字符，生成所有可能的前身：

```text
predecessor = word[:i] + word[i + 1:]
```

如果 `predecessor` 已经出现在 `dp` 中，则可以把对应的链延长一层：

```text
dp[word] = max(dp[word], dp[predecessor] + 1)
```

遍历过程中维护所有 `dp[word]` 的最大值即可。

### 为什么删除一个字符能枚举所有前身

题目定义中，前身通过插入一个字符得到当前单词。反过来看，从当前单词删除被插入的那个字符，就会还原前身。因此删除当前单词的每个位置，恰好覆盖全部可能的前身。

## 复杂度分析

设单词数量为 `n`，最长单词长度为 `L`：

- 排序时间复杂度：`O(n log n)`。
- 动态规划时间复杂度：`O(nL²)`；每个单词尝试删除 `L` 个位置，每次构造新字符串需要 `O(L)`。
- 空间复杂度：`O(nL)`，用于哈希表保存单词及其状态。

题目中 `L <= 16`，该方法可以高效通过。

## 容易踩坑点

- 前身必须恰好少一个字符，不是替换字符，也不能打乱原字符顺序。
- 必须按字符串长度升序处理，否则前身状态可能尚未计算。
- 单个单词本身就是长度为 `1` 的字符串链。
- 删除字符后只有当所得字符串在输入单词集合中出现，才能进行状态转移。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    int longestStrChain(vector<string>& words) {
        sort(words.begin(), words.end(), [](const string& a, const string& b) {
            return a.size() < b.size();
        });

        unordered_map<string, int> dp;
        int answer = 0;

        for (const string& word : words) {
            int best = 1;
            for (int i = 0; i < static_cast<int>(word.size()); ++i) {
                string predecessor = word.substr(0, i) + word.substr(i + 1);
                auto it = dp.find(predecessor);
                if (it != dp.end()) {
                    best = max(best, it->second + 1);
                }
            }
            dp[word] = best;
            answer = max(answer, best);
        }
        return answer;
    }
};
```

## LeetCode 可提交解法：Python 3

```python
class Solution:
    def longestStrChain(self, words: list[str]) -> int:
        words.sort(key=len)
        dp: dict[str, int] = {}
        answer = 0

        for word in words:
            best = 1
            for i in range(len(word)):
                predecessor = word[:i] + word[i + 1:]
                best = max(best, dp.get(predecessor, 0) + 1)
            dp[word] = best
            answer = max(answer, best)

        return answer
```

## ACM 答题模式

### 输入格式

第一行输入整数 `n`，表示单词数量。

接下来输入 `n` 个仅包含小写英文字母的单词，单词之间使用空白字符分隔。

```text
n
word1 word2 ... wordn
```

### 输出格式

输出最长字符串链的长度。

### ACM 模式 C++ 完整代码

```cpp
#include <algorithm>
#include <iostream>
#include <string>
#include <unordered_map>
#include <vector>
using namespace std;

int longestStringChain(vector<string>& words) {
    sort(words.begin(), words.end(), [](const string& a, const string& b) {
        return a.size() < b.size();
    });

    unordered_map<string, int> dp;
    int answer = 0;

    for (const string& word : words) {
        int best = 1;
        for (int i = 0; i < static_cast<int>(word.size()); ++i) {
            string predecessor = word.substr(0, i) + word.substr(i + 1);
            auto it = dp.find(predecessor);
            if (it != dp.end()) {
                best = max(best, it->second + 1);
            }
        }
        dp[word] = best;
        answer = max(answer, best);
    }
    return answer;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    vector<string> words(n);
    for (string& word : words) {
        cin >> word;
    }

    cout << longestStringChain(words) << '\n';
    return 0;
}
```

### ACM 模式 Python 3 完整代码

```python
import sys


def longest_string_chain(words: list[str]) -> int:
    words.sort(key=len)
    dp: dict[str, int] = {}
    answer = 0

    for word in words:
        best = 1
        for i in range(len(word)):
            predecessor = word[:i] + word[i + 1:]
            best = max(best, dp.get(predecessor, 0) + 1)
        dp[word] = best
        answer = max(answer, best)

    return answer


def main() -> None:
    data = sys.stdin.buffer.read().split()
    if not data:
        return

    n = int(data[0])
    words = [word.decode() for word in data[1:1 + n]]
    print(longest_string_chain(words))


if __name__ == "__main__":
    main()
```

## 示例与自测

### 示例 1

```text
输入：
6
a b ba bca bda bdca

输出：
4
```

一条最长字符串链是：`a → ba → bda → bdca`。

### 示例 2

```text
输入：
5
xbc pcxbcf xb cxbc pcxbc

输出：
5
```

### 示例 3

```text
输入：
2
abcd dbqca

输出：
1
```

### 边界用例

```text
输入：
1
hello

输出：
1
```
