# 2026-09-15｜LeetCode 141/142. 环形链表（数组关系改版）

> **题目标签：LeetCode 热题 100｜链表｜快慢指针｜状态标记**

## 对应的 LeetCode 原题

这道题的描述来源于：

- [LeetCode 141. 环形链表](https://leetcode.cn/problems/linked-list-cycle/)：判断链表中是否存在环。
- [LeetCode 142. 环形链表 II](https://leetcode.cn/problems/linked-list-cycle-ii/)：返回链表开始入环的第一个节点，无环时返回 `null`。

题目沿用了 LeetCode 141 对“环”的定义，但要求返回入环节点，因此返回目标实际上更接近 **LeetCode 142**。此外，本题没有直接给出链表节点，而是用数组保存每个节点的后继节点编号。

## 改版题目

给定整数数组 `head`：

```text
head[index] = value
```

表示编号为 `index` 的节点指向编号为 `value` 的节点，节点编号从 `0` 开始。

如果 `value` 不在 `[0, head.size() - 1]` 范围内，表示当前节点没有后继节点，相当于链表中的 `null`。

如果沿着节点的链接关系不断前进，能够再次到达当前路径中已经访问过的节点，则存在环。返回开始进入该环的节点编号；如果不存在环，返回 `null`。

由于数组可能表示多个互不连通的链，本题按节点编号从小到大检查各条链，返回第一条被发现的环的入口节点。

### 样例

```text
head = [3, 2, 1, 4]
```

表示：

```text
0 -> 3 -> 4 -> null
1 -> 2 -> 1 -> ...
```

节点 `1` 和节点 `2` 构成环。从节点 `1` 开始跟踪时，第一个再次到达的节点是 `1`，因此返回：

```text
1
```

## 关键思路：访问轮次标记

标准 LeetCode 142 只有一个链表头，可以直接使用 Floyd 快慢指针。但本题的数组可能包含多个互不连通的部分，而且没有单独给出起始节点，因此需要依次检查所有尚未处理的节点。

使用两个数组：

- `visitRound[i]`：节点 `i` 是在哪一轮搜索中被访问的。
- `done[i]`：节点 `i` 所在的路径已经完整处理过。

从每个尚未处理的节点 `start` 出发：

1. 为本轮搜索分配唯一编号 `round = start + 1`。
2. 沿着 `head[current]` 不断前进。
3. 如果走出合法下标范围，当前路径无环。
4. 如果到达已经完成处理的节点，当前路径不会产生新的环。
5. 如果再次遇到本轮访问过的节点，即 `visitRound[current] == round`，当前节点就是这次遍历的入环节点。
6. 对无环路径进行收尾，把本轮经过的节点标记为 `done`，避免后续重复遍历。

## 为什么重复节点就是入环节点

一条路径在进入环之前不会访问重复节点。第一次遇到本轮已经访问过的节点时，说明路径刚好绕环一周回到了入口；因此这个重复节点就是相对于本次起点的入环节点。

全局已经处理完的路径无需再次检查。每个节点最多在主遍历和收尾遍历中各经过一次，因此整体仍是线性复杂度。

## 复杂度分析

设节点数量为 `n`：

- 时间复杂度：`O(n)`。
- 空间复杂度：`O(n)`。

> 如果题目改为标准 LeetCode 142，并明确给出唯一链表头，则 Floyd 快慢指针可以把额外空间降到 `O(1)`。

## 容易踩坑点

- 题目要求返回入口节点，不只是判断是否有环；这一点更接近 LeetCode 142。
- 示例中的值 `4` 超出了长度为 `4` 的数组下标范围，应视为 `null`。
- 数组可能包含多个互不连通的链，不能只从节点 `0` 开始检查，否则样例中的 `1 -> 2 -> 1` 会被漏掉。
- 必须区分“本轮访问过”与“以前的搜索访问过”。只有重新遇到本轮路径中的节点才表示发现了当前环。
- 自环也是合法的环，例如 `head = [0]` 应返回 `0`。

## 数组改版解法：C++

```cpp
#include <vector>
using namespace std;

int findCycleEntry(const vector<int>& head) {
    int n = static_cast<int>(head.size());
    vector<int> visitRound(n, 0);
    vector<char> done(n, 0);

    for (int start = 0; start < n; ++start) {
        if (done[start]) {
            continue;
        }

        int round = start + 1;
        int current = start;

        while (current >= 0 && current < n &&
               !done[current] && visitRound[current] != round) {
            visitRound[current] = round;
            current = head[current];
        }

        if (current >= 0 && current < n &&
            !done[current] && visitRound[current] == round) {
            return current;
        }

        current = start;
        while (current >= 0 && current < n &&
               !done[current] && visitRound[current] == round) {
            done[current] = 1;
            current = head[current];
        }
    }

    return -1;
}
```

C++ 函数使用 `-1` 表示题目中的 `null`。

## 数组改版解法：Python 3

```python
def find_cycle_entry(head: list[int]) -> int | None:
    n = len(head)
    visit_round = [0] * n
    done = [False] * n

    for start in range(n):
        if done[start]:
            continue

        round_id = start + 1
        current = start

        while (
            0 <= current < n
            and not done[current]
            and visit_round[current] != round_id
        ):
            visit_round[current] = round_id
            current = head[current]

        if (
            0 <= current < n
            and not done[current]
            and visit_round[current] == round_id
        ):
            return current

        current = start
        while (
            0 <= current < n
            and not done[current]
            and visit_round[current] == round_id
        ):
            done[current] = True
            current = head[current]

    return None
```

## 标准 LeetCode 142 解法：C++

如果面试官改为标准链表结构并给定唯一的 `head`，可以使用 Floyd 快慢指针：

```cpp
class Solution {
public:
    ListNode* detectCycle(ListNode* head) {
        ListNode* slow = head;
        ListNode* fast = head;

        do {
            if (fast == nullptr || fast->next == nullptr) {
                return nullptr;
            }
            slow = slow->next;
            fast = fast->next->next;
        } while (slow != fast);

        slow = head;
        while (slow != fast) {
            slow = slow->next;
            fast = fast->next;
        }
        return slow;
    }
};
```

## 标准 LeetCode 142 解法：Python 3

```python
class Solution:
    def detectCycle(self, head: Optional[ListNode]) -> Optional[ListNode]:
        slow = head
        fast = head

        while fast is not None and fast.next is not None:
            slow = slow.next
            fast = fast.next.next
            if slow is fast:
                break
        else:
            return None

        slow = head
        while slow is not fast:
            slow = slow.next
            fast = fast.next
        return slow
```

## ACM 答题模式

### 输入格式

第一行输入整数 `n`，表示节点数量。

第二行输入 `n` 个整数，其中第 `i` 个整数表示节点 `i` 指向的节点编号。小于 `0` 或大于等于 `n` 的值表示 `null`。

```text
n
head[0] head[1] ... head[n-1]
```

### 输出格式

- 如果存在环，输出按节点编号从小到大扫描时，第一条被发现的环的入口节点编号。
- 如果不存在环，输出 `null`。

### ACM 模式 C++ 完整代码

```cpp
#include <iostream>
#include <vector>
using namespace std;

int findCycleEntry(const vector<int>& head) {
    int n = static_cast<int>(head.size());
    vector<int> visitRound(n, 0);
    vector<char> done(n, 0);

    for (int start = 0; start < n; ++start) {
        if (done[start]) {
            continue;
        }

        int round = start + 1;
        int current = start;

        while (current >= 0 && current < n &&
               !done[current] && visitRound[current] != round) {
            visitRound[current] = round;
            current = head[current];
        }

        if (current >= 0 && current < n &&
            !done[current] && visitRound[current] == round) {
            return current;
        }

        current = start;
        while (current >= 0 && current < n &&
               !done[current] && visitRound[current] == round) {
            done[current] = 1;
            current = head[current];
        }
    }

    return -1;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    vector<int> head(n);
    for (int& next : head) {
        cin >> next;
    }

    int entry = findCycleEntry(head);
    if (entry == -1) {
        cout << "null\n";
    } else {
        cout << entry << '\n';
    }
    return 0;
}
```

### ACM 模式 Python 3 完整代码

```python
import sys


def find_cycle_entry(head: list[int]) -> int | None:
    n = len(head)
    visit_round = [0] * n
    done = [False] * n

    for start in range(n):
        if done[start]:
            continue

        round_id = start + 1
        current = start

        while (
            0 <= current < n
            and not done[current]
            and visit_round[current] != round_id
        ):
            visit_round[current] = round_id
            current = head[current]

        if (
            0 <= current < n
            and not done[current]
            and visit_round[current] == round_id
        ):
            return current

        current = start
        while (
            0 <= current < n
            and not done[current]
            and visit_round[current] == round_id
        ):
            done[current] = True
            current = head[current]

    return None


def main() -> None:
    data = list(map(int, sys.stdin.buffer.read().split()))
    if not data:
        return

    n = data[0]
    head = data[1:1 + n]
    entry = find_cycle_entry(head)
    print("null" if entry is None else entry)


if __name__ == "__main__":
    main()
```

## 示例与自测

### 题目样例

```text
输入：
4
3 2 1 4

输出：
1
```

### 无环

```text
输入：
4
1 2 3 4

输出：
null
```

### 自环

```text
输入：
1
0

输出：
0
```

### 链表进入环

```text
输入：
4
1 2 3 1

输出：
1
```
