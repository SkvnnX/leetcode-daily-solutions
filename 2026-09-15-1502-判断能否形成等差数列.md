# LeetCode 1502：判断能否形成等差数列

> **题目来源标记：华为 OD 面试原题**

## 基本信息

- 题号：1502
- 题名：判断能否形成等差数列
- 英文题名：Can Make Arithmetic Progression From Sequence
- 难度：简单
- 标签/考点：数组、排序、等差数列
- 题目链接：https://leetcode.cn/problems/can-make-arithmetic-progression-from-sequence/

## 题意概括

给定一个整数数组 `arr`，可以任意调整元素顺序。判断能否把它重新排列成等差数列，即排序后任意相邻两项之差都相等。

## 关键思路

把数组从小到大排序。排序后，如果这些数能组成等差数列，那么公差只能是前两项之差：

```text
d = arr[1] - arr[0]
```

随后检查每一对相邻元素是否都满足：

```text
arr[i] - arr[i - 1] == d
```

只要有一处不相等，就无法形成等差数列；全部相等则可以。

### 为什么排序后检查即可

若数组可以重新排列成等差数列，将这个等差数列按升序排列后，元素仍然构成等差数列，而且相邻差为非负常数。因此只需检查数组唯一确定的升序排列，不需要枚举所有排列。

重复元素也不需要特殊处理：如果所有元素都相同，排序后公差为 `0`，依然是合法等差数列。

## 复杂度分析

- 时间复杂度：`O(n log n)`，主要来自排序。
- 空间复杂度：C++ 的排序通常使用 `O(log n)` 栈空间；Python 的 `sorted` 需要 `O(n)` 额外空间。

## 容易踩坑点

- 题目允许重新排列，不能直接按原数组顺序判断。
- 公差可以为 `0`，例如 `[7, 7, 7]` 可以形成等差数列。
- 数组至少有两个元素，因此可以直接使用排序后的前两项确定公差。
- ACM 模式下先读取数组长度 `n`，再读取恰好 `n` 个整数。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    bool canMakeArithmeticProgression(vector<int>& arr) {
        sort(arr.begin(), arr.end());
        int difference = arr[1] - arr[0];

        for (int i = 2; i < static_cast<int>(arr.size()); ++i) {
            if (arr[i] - arr[i - 1] != difference) {
                return false;
            }
        }
        return true;
    }
};
```

## LeetCode 可提交解法：Python 3

```python
class Solution:
    def canMakeArithmeticProgression(self, arr: list[int]) -> bool:
        arr.sort()
        difference = arr[1] - arr[0]

        for i in range(2, len(arr)):
            if arr[i] - arr[i - 1] != difference:
                return False
        return True
```

## ACM 答题模式

### 输入格式

第一行输入整数 `n`，表示数组长度。

第二行输入 `n` 个整数，表示数组 `arr`。

```text
n
arr1 arr2 ... arrn
```

### 输出格式

如果数组可以重新排列成等差数列，输出 `true`；否则输出 `false`。

### ACM 模式 C++ 完整代码

```cpp
#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

bool canMakeArithmeticProgression(vector<int>& arr) {
    sort(arr.begin(), arr.end());
    int difference = arr[1] - arr[0];

    for (int i = 2; i < static_cast<int>(arr.size()); ++i) {
        if (arr[i] - arr[i - 1] != difference) {
            return false;
        }
    }
    return true;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    vector<int> arr(n);
    for (int& value : arr) {
        cin >> value;
    }

    cout << (canMakeArithmeticProgression(arr) ? "true" : "false") << '\n';
    return 0;
}
```

### ACM 模式 Python 3 完整代码

```python
import sys


def can_make_arithmetic_progression(arr: list[int]) -> bool:
    arr.sort()
    difference = arr[1] - arr[0]

    for i in range(2, len(arr)):
        if arr[i] - arr[i - 1] != difference:
            return False
    return True


def main() -> None:
    data = list(map(int, sys.stdin.buffer.read().split()))
    if not data:
        return

    n = data[0]
    arr = data[1:1 + n]
    print("true" if can_make_arithmetic_progression(arr) else "false")


if __name__ == "__main__":
    main()
```

## 示例与自测

### 示例 1

```text
输入：
3
3 5 1

输出：
true
```

排序后为 `[1, 3, 5]`，相邻差均为 `2`。

### 示例 2

```text
输入：
3
1 2 4

输出：
false
```

排序后相邻差分别为 `1` 和 `2`。

### 边界用例：公差为 0

```text
输入：
4
7 7 7 7

输出：
true
```

### 边界用例：包含负数

```text
输入：
4
9 -3 5 1

输出：
true
```

排序后为 `[-3, 1, 5, 9]`，相邻差均为 `4`。
