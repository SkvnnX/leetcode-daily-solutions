# 2026-09-15 LeetCode 每日一题：2472. 不重叠回文子字符串的最大数目

## 基本信息

- 日期：2026-09-15
- 题号：2472
- 题名：不重叠回文子字符串的最大数目
- 难度：困难
- 标签/考点：贪心、双指针、字符串、动态规划、回文判定
- 题目链接：https://leetcode.cn/problems/maximum-number-of-non-overlapping-palindrome-substrings/

## 题意概括

给定字符串 `s` 和正整数 `k`，需要从 `s` 中选择尽可能多的子字符串，要求每个子字符串都是回文串、长度至少为 `k`，并且任意两个被选中的子字符串不能重叠。返回最多能选择多少个。

## 关键思路

先预处理任意区间 `s[l..r]` 是否为回文串，然后做前缀动态规划：令 `dp[i]` 表示只考虑前 `i` 个字符 `s[0..i-1]` 时最多能选多少个合法回文子串。

转移时有两种选择：

1. 不选以 `i - 1` 结尾的子串，继承 `dp[i - 1]`。
2. 选一个以 `i - 1` 结尾、长度至少为 `k` 的回文子串 `s[l..i-1]`，答案候选为 `dp[l] + 1`。

关键优化是：只需要检查长度为 `k` 和 `k + 1` 的回文串。原因是如果存在一个长度 `L >= k + 2` 的回文串，那么去掉首尾字符后，中间仍是回文串，长度为 `L - 2 >= k`。选择更短的内部回文串不会减少可选数量，反而会留下更多空间给左右两边。因此在最优贪心/DP 选择中，总能把每次选择压缩到长度 `k` 或 `k + 1`。

这也是本题“为什么可以贪心地选尽早结束的短回文”的核心：在同一个结尾附近，越短的合法回文占用越少，对后续选择越有利；动态规划用 `dp[l] + 1` 保证了左侧部分也已经最优。

## 复杂度分析

- 预处理回文区间：`O(n^2)` 时间，`O(n^2)` 空间。
- 动态规划转移：每个位置只检查长度 `k` 和 `k + 1`，共 `O(n)` 时间。
- 总复杂度：`O(n^2)` 时间，`O(n^2)` 空间。
- 其中 `n = len(s)`，题目约束 `n <= 2000`，可以接受。

## 容易踩坑点

- 不能简单统计所有回文子串，因为题目要求“不重叠”。
- 不需要枚举所有长度 `>= k` 的回文作为转移，否则虽然 `n=2000` 也可能勉强能过，但思路不够精炼。
- `dp[i]` 的 `i` 建议表示前缀长度，不是字符下标；这样 `s[l..i-1]` 对应左侧答案 `dp[l]`，边界更清楚。
- 长度 `k + 1` 必须检查：奇偶长度回文的中心不同，只看长度 `k` 会漏解。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    int maxPalindromes(string s, int k) {
        int n = (int)s.size();
        vector<vector<char>> pal(n, vector<char>(n, 0));
        for (int i = n - 1; i >= 0; --i) {
            for (int j = i; j < n; ++j) {
                if (s[i] == s[j] && (j - i < 2 || pal[i + 1][j - 1])) {
                    pal[i][j] = 1;
                }
            }
        }

        vector<int> dp(n + 1, 0);
        for (int i = 1; i <= n; ++i) {
            dp[i] = dp[i - 1];
            for (int len : {k, k + 1}) {
                int l = i - len;
                if (l >= 0 && pal[l][i - 1]) {
                    dp[i] = max(dp[i], dp[l] + 1);
                }
            }
        }
        return dp[n];
    }
};
```

## LeetCode 可提交解法：Python 3

```python
class Solution:
    def maxPalindromes(self, s: str, k: int) -> int:
        n = len(s)
        pal = [[False] * n for _ in range(n)]
        for i in range(n - 1, -1, -1):
            for j in range(i, n):
                if s[i] == s[j] and (j - i < 2 or pal[i + 1][j - 1]):
                    pal[i][j] = True

        dp = [0] * (n + 1)
        for i in range(1, n + 1):
            dp[i] = dp[i - 1]
            for length in (k, k + 1):
                l = i - length
                if l >= 0 and pal[l][i - 1]:
                    dp[i] = max(dp[i], dp[l] + 1)
        return dp[n]
```

## ACM 答题模式

### 自定义输入格式

输入共一行或两行均可，按空白分隔读取：

```text
s k
```

- `s`：只包含小写英文字母的字符串。
- `k`：每个回文子字符串的最小长度。

输出一个整数，表示最多能选择的不重叠合法回文子字符串数量。

### ACM 模式 C++ 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int maxPalindromes(const string& s, int k) {
    int n = (int)s.size();
    vector<vector<char>> pal(n, vector<char>(n, 0));
    for (int i = n - 1; i >= 0; --i) {
        for (int j = i; j < n; ++j) {
            if (s[i] == s[j] && (j - i < 2 || pal[i + 1][j - 1])) {
                pal[i][j] = 1;
            }
        }
    }

    vector<int> dp(n + 1, 0);
    for (int i = 1; i <= n; ++i) {
        dp[i] = dp[i - 1];
        for (int len : {k, k + 1}) {
            int l = i - len;
            if (l >= 0 && pal[l][i - 1]) {
                dp[i] = max(dp[i], dp[l] + 1);
            }
        }
    }
    return dp[n];
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string s;
    int k;
    cin >> s >> k;
    cout << maxPalindromes(s, k) << '\n';
    return 0;
}
```

### ACM 模式 Python 3 完整代码

```python
import sys


def max_palindromes(s: str, k: int) -> int:
    n = len(s)
    pal = [[False] * n for _ in range(n)]
    for i in range(n - 1, -1, -1):
        for j in range(i, n):
            if s[i] == s[j] and (j - i < 2 or pal[i + 1][j - 1]):
                pal[i][j] = True

    dp = [0] * (n + 1)
    for i in range(1, n + 1):
        dp[i] = dp[i - 1]
        for length in (k, k + 1):
            l = i - length
            if l >= 0 and pal[l][i - 1]:
                dp[i] = max(dp[i], dp[l] + 1)
    return dp[n]


def main() -> None:
    data = sys.stdin.read().split()
    if not data:
        return
    s = data[0]
    k = int(data[1])
    print(max_palindromes(s, k))


if __name__ == "__main__":
    main()
```

## 示例与自测

```text
输入：abaccdbbd 3
输出：2
```

```text
输入：adbcda 2
输出：0
```

边界用例：

```text
输入：a 1
输出：1
```

```text
输入：aaaaa 2
输出：2
```
