# 2026-09-16 LeetCode 每日一题：1621. 大小为 K 的不重叠线段的数目

- 日期：2026-09-16
- 题号：1621
- 题名：大小为 K 的不重叠线段的数目
- 英文题名：Number of Sets of K Non-Overlapping Line Segments
- 难度：Medium（中等）
- 标签/考点：数学、动态规划、组合数学、前缀和
- 题目链接：https://leetcode.cn/problems/number-of-sets-of-k-non-overlapping-line-segments/ （LeetCode: https://leetcode.com/problems/number-of-sets-of-k-non-overlapping-line-segments/）

## 题意概括

在一维坐标轴上有 `n` 个点，编号和坐标都是 `0` 到 `n - 1`。要求画出恰好 `k` 条互不重叠的线段，每条线段至少覆盖两个点，也就是端点必须是两个不同的整数点。线段不需要覆盖所有点，并且不同线段允许共用端点。返回方案数，对 `1e9 + 7` 取模。

例如 `n = 4, k = 2` 时，答案为 `5`，因为可以选择的两条线段组合包括 `(0,2)+(2,3)`、`(0,1)+(1,3)` 等。

## 关键思路：把“共享端点”转成组合计数

直接枚举线段端点会遇到一个细节：相邻两条线段可以共用端点，例如 `[0,1]` 和 `[1,2]` 是合法的。这个“可以相等”的边界让普通的从 `2k` 个不同端点中选择不够直接。

可以用一个经典等价变换：

1. 设第 `i` 条线段为 `[l_i, r_i]`，满足：
   - `l_i < r_i`，保证线段至少覆盖两个点；
   - `r_i <= l_(i+1)`，保证不重叠且允许共用端点。
2. 如果相邻线段共用端点，普通端点序列里会出现相等。为了把它转成严格递增的选择，可以给第 `i` 条线段的两个端点整体加上偏移 `i`。
3. 于是原问题与从 `n + k - 1` 个位置中选出 `2k` 个端点一一对应。

因此答案是：

```text
C(n + k - 1, 2k) mod (1e9 + 7)
```

由于 `n <= 1000`，`n + k - 1` 最大不到 `2000`，可以预处理阶乘和逆元后直接计算组合数。

## 为什么这样做是正确的

把每条线段的两个端点按顺序写成：

```text
l_1, r_1, l_2, r_2, ..., l_k, r_k
```

合法条件是每个 `l_i < r_i`，并且 `r_i <= l_(i+1)`。唯一可能“不严格递增”的位置就是相邻线段共用端点，即 `r_i = l_(i+1)`。

给第 `i` 条线段前面的端点增加与线段编号相关的偏移后，相等边界会被拉开，所有被选位置变成严格递增。反过来，从 `n + k - 1` 个位置中选出 `2k` 个严格递增的位置，也能唯一还原成原来的 `k` 条合法线段。所以方案数就是组合数。

## 复杂度分析

- 时间复杂度：`O(n + k)`，预处理阶乘和逆阶乘。
- 空间复杂度：`O(n + k)`。

## 容易踩坑点

1. 线段允许共享端点，例如 `[0,1]` 和 `[1,2]` 可以同时选。
2. 每条线段必须至少覆盖两个点，所以左右端点不能相同。
3. 返回值要对 `1e9 + 7` 取模。
4. 组合数公式是 `C(n + k - 1, 2k)`，不是 `C(n, 2k)`。
5. `k` 最大可接近 `n - 1`，但 `n + k - 1` 仍然很小，阶乘预处理足够。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    static constexpr int MOD = 1000000007;

    long long modPow(long long a, long long e) {
        long long ans = 1;
        while (e > 0) {
            if (e & 1) ans = ans * a % MOD;
            a = a * a % MOD;
            e >>= 1;
        }
        return ans;
    }

    int numberOfSets(int n, int k) {
        int N = n + k - 1;
        int R = 2 * k;
        vector<long long> fact(N + 1), invFact(N + 1);
        fact[0] = 1;
        for (int i = 1; i <= N; ++i) fact[i] = fact[i - 1] * i % MOD;

        invFact[N] = modPow(fact[N], MOD - 2);
        for (int i = N; i >= 1; --i) invFact[i - 1] = invFact[i] * i % MOD;

        return (int)(fact[N] * invFact[R] % MOD * invFact[N - R] % MOD);
    }
};
```

## LeetCode 可提交解法：Python 3

```python
class Solution:
    def numberOfSets(self, n: int, k: int) -> int:
        MOD = 10 ** 9 + 7
        N = n + k - 1
        R = 2 * k

        fact = [1] * (N + 1)
        for i in range(1, N + 1):
            fact[i] = fact[i - 1] * i % MOD

        inv_fact = [1] * (N + 1)
        inv_fact[N] = pow(fact[N], MOD - 2, MOD)
        for i in range(N, 0, -1):
            inv_fact[i - 1] = inv_fact[i] * i % MOD

        return fact[N] * inv_fact[R] % MOD * inv_fact[N - R] % MOD
```

## ACM 答题模式

### 自定义输入格式

输入两个整数：

```text
n k
```

输出一个整数，表示恰好画出 `k` 条不重叠线段的方案数，结果对 `1e9 + 7` 取模。

示例：

```text
4 2
```

输出：

```text
5
```

### ACM 模式 C++ 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

static const int MOD = 1000000007;

long long modPow(long long a, long long e) {
    long long ans = 1;
    while (e > 0) {
        if (e & 1) ans = ans * a % MOD;
        a = a * a % MOD;
        e >>= 1;
    }
    return ans;
}

long long comb(int n, int r) {
    if (r < 0 || r > n) return 0;
    vector<long long> fact(n + 1), invFact(n + 1);
    fact[0] = 1;
    for (int i = 1; i <= n; ++i) fact[i] = fact[i - 1] * i % MOD;
    invFact[n] = modPow(fact[n], MOD - 2);
    for (int i = n; i >= 1; --i) invFact[i - 1] = invFact[i] * i % MOD;
    return fact[n] * invFact[r] % MOD * invFact[n - r] % MOD;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, k;
    if (!(cin >> n >> k)) return 0;

    cout << comb(n + k - 1, 2 * k) << '
';
    return 0;
}
```

### ACM 模式 Python 3 完整代码

```python
import sys

MOD = 10 ** 9 + 7


def comb(n: int, r: int) -> int:
    if r < 0 or r > n:
        return 0
    fact = [1] * (n + 1)
    for i in range(1, n + 1):
        fact[i] = fact[i - 1] * i % MOD

    inv_fact = [1] * (n + 1)
    inv_fact[n] = pow(fact[n], MOD - 2, MOD)
    for i in range(n, 0, -1):
        inv_fact[i - 1] = inv_fact[i] * i % MOD

    return fact[n] * inv_fact[r] % MOD * inv_fact[n - r] % MOD


def main() -> None:
    data = sys.stdin.read().strip().split()
    if not data:
        return
    n, k = map(int, data[:2])
    print(comb(n + k - 1, 2 * k))


if __name__ == "__main__":
    main()
```

## 示例与边界用例

| n | k | 输出 | 说明 |
|---:|---:|---:|---|
| 4 | 2 | 5 | 官方示例 1 |
| 3 | 1 | 3 | 官方示例 2 |
| 30 | 7 | 796297179 | 官方示例 3 |
| 2 | 1 | 1 | 只有一条线段 `[0,1]` |
| 5 | 4 | 1 | 每两个相邻点各选一条线段，全部共享端点链式连接 |
