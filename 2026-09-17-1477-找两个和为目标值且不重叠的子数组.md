# 2026-09-17 LeetCode 每日一题：1477. 找两个和为目标值且不重叠的子数组

## 基本信息

- 日期：2026-09-17
- 题号：1477
- 题名：找两个和为目标值且不重叠的子数组
- 英文题名：Find Two Non-overlapping Sub-arrays Each With Target Sum
- 难度：中等
- 标签/考点：数组、哈希表、二分查找、动态规划、滑动窗口
- 题目链接：https://leetcode.cn/problems/find-two-non-overlapping-sub-arrays-each-with-target-sum/

## 题意概括

给定正整数数组 `arr` 和整数 `target`，需要找到两个互不重叠的连续子数组，且两个子数组的和都等于 `target`。在所有合法方案中，返回两个子数组长度之和的最小值；如果不存在这样的两个子数组，返回 `-1`。

## 关键思路：滑动窗口 + 前缀最优

因为 `arr[i]` 都是正整数，可以用滑动窗口在线性时间内枚举所有和为 `target` 的子数组。

同时维护一个前缀最优数组思想：

- `best`：到当前扫描位置之前，已经出现过的、和为 `target` 的子数组的最短长度。
- `min_len_ending_at[i]`：可以理解为截至下标 `i` 为止的最短合法子数组长度；代码中用变量滚动维护即可。

当滑动窗口 `[left, right]` 的和等于 `target` 时：

1. 当前窗口长度为 `current_len = right - left + 1`。
2. 如果在 `left` 之前已经存在一个合法子数组，那么它一定与当前窗口不重叠，可以用 `best_before[left] + current_len` 更新答案。
3. 再用当前窗口长度更新截至 `right` 的最短合法子数组长度。

为了写得更清晰，可以使用数组 `best[i]`：表示在 `arr[0..i-1]` 中，和为 `target` 的最短子数组长度。这样当前窗口 `[left, right]` 与左侧不重叠的最优子数组就是 `best[left]`。

## 为什么这样不会重叠

当前窗口从 `left` 开始，因此任何完全落在 `arr[0..left-1]` 的子数组都与它不重叠。`best[left]` 只统计前 `left` 个元素中的合法子数组，所以用 `best[left] + current_len` 更新答案一定满足“不重叠”。

滑动窗口按右端点从左到右枚举所有和为 `target` 的子数组，`best` 又记录每个位置左侧的最短合法子数组，因此不会漏掉最优组合。

## 复杂度分析

- 时间复杂度：`O(n)`，左右指针都最多移动 `n` 次。
- 空间复杂度：`O(n)`，用于保存前缀最短长度数组。

## 容易踩坑点

- 题目要求两个子数组都连续，并且不能重叠。
- 返回的是两个子数组长度之和，不是子数组和。
- `arr[i]` 为正整数，所以滑动窗口成立；如果数组允许负数，则要改用前缀和哈希表。
- `best[left]` 表示当前窗口左边的最优答案，不能用包含当前窗口的状态，否则会产生重叠。
- 不存在两个合法子数组时返回 `-1`。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    int minSumOfLengths(vector<int>& arr, int target) {
        const int INF = 1e9;
        int n = arr.size();
        vector<int> best(n + 1, INF);

        int left = 0;
        int sum = 0;
        int answer = INF;

        for (int right = 0; right < n; ++right) {
            sum += arr[right];

            while (sum > target) {
                sum -= arr[left];
                ++left;
            }

            best[right + 1] = best[right];

            if (sum == target) {
                int currentLen = right - left + 1;
                if (best[left] != INF) {
                    answer = min(answer, best[left] + currentLen);
                }
                best[right + 1] = min(best[right + 1], currentLen);
            }
        }

        return answer == INF ? -1 : answer;
    }
};
```

## LeetCode 可提交解法：Python 3

```python
class Solution:
    def minSumOfLengths(self, arr: list[int], target: int) -> int:
        inf = 10 ** 9
        n = len(arr)
        best = [inf] * (n + 1)

        left = 0
        window_sum = 0
        answer = inf

        for right, value in enumerate(arr):
            window_sum += value

            while window_sum > target:
                window_sum -= arr[left]
                left += 1

            best[right + 1] = best[right]

            if window_sum == target:
                current_len = right - left + 1
                if best[left] != inf:
                    answer = min(answer, best[left] + current_len)
                best[right + 1] = min(best[right + 1], current_len)

        return -1 if answer == inf else answer
```

## ACM 答题模式

### 输入格式

第一行输入两个整数 `n` 和 `target`，分别表示数组长度和目标和。

第二行输入 `n` 个正整数，表示数组 `arr`。

```text
n target
arr1 arr2 ... arrn
```

### 输出格式

输出两个不重叠、和为 `target` 的子数组的最小长度和；不存在则输出 `-1`。

### ACM 模式 C++ 完整代码

```cpp
#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

int minSumOfLengths(vector<int>& arr, int target) {
    const int INF = 1e9;
    int n = arr.size();
    vector<int> best(n + 1, INF);

    int left = 0;
    int sum = 0;
    int answer = INF;

    for (int right = 0; right < n; ++right) {
        sum += arr[right];

        while (sum > target) {
            sum -= arr[left];
            ++left;
        }

        best[right + 1] = best[right];

        if (sum == target) {
            int currentLen = right - left + 1;
            if (best[left] != INF) {
                answer = min(answer, best[left] + currentLen);
            }
            best[right + 1] = min(best[right + 1], currentLen);
        }
    }

    return answer == INF ? -1 : answer;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, target;
    cin >> n >> target;

    vector<int> arr(n);
    for (int& value : arr) {
        cin >> value;
    }

    cout << minSumOfLengths(arr, target) << '\n';
    return 0;
}
```

### ACM 模式 Python 3 完整代码

```python
import sys


def min_sum_of_lengths(arr: list[int], target: int) -> int:
    inf = 10 ** 9
    n = len(arr)
    best = [inf] * (n + 1)

    left = 0
    window_sum = 0
    answer = inf

    for right, value in enumerate(arr):
        window_sum += value

        while window_sum > target:
            window_sum -= arr[left]
            left += 1

        best[right + 1] = best[right]

        if window_sum == target:
            current_len = right - left + 1
            if best[left] != inf:
                answer = min(answer, best[left] + current_len)
            best[right + 1] = min(best[right + 1], current_len)

    return -1 if answer == inf else answer


def main() -> None:
    data = list(map(int, sys.stdin.buffer.read().split()))
    if not data:
        return

    n, target = data[0], data[1]
    arr = data[2:2 + n]
    print(min_sum_of_lengths(arr, target))


if __name__ == "__main__":
    main()
```

## 示例与自测

### 示例 1

```text
输入：
5 3
3 2 2 4 3

输出：
2
```

两个合法子数组是开头的 `[3]` 和结尾的 `[3]`。

### 示例 2

```text
输入：
4 7
7 3 4 7

输出：
2
```

选择两个长度为 `1` 的 `[7]`。

### 示例 3

```text
输入：
7 6
4 3 2 6 2 3 4

输出：
-1
```

只有一个和为 `6` 的子数组，无法选出两个。

### 边界用例

```text
输入：
3 2
1 1 1

输出：
-1
```

两个和为 `2` 的子数组 `[1,1]` 会重叠，不能同时选择。
