# 2026-09-18 LeetCode 每日一题：1520. 最多的不重叠子字符串

- 日期：2026-09-18
- 题号：1520
- 题名：最多的不重叠子字符串（Maximum Number of Non-Overlapping Substrings）
- 难度：困难
- 标签/考点：贪心、哈希表、字符串、排序、区间选择
- 题目链接：https://leetcode.cn/problems/maximum-number-of-non-overlapping-substrings/
- 官方来源：力扣 GraphQL `todayRecord`（本地日期 2026-09-18）。

## 题意概括

给定一个只包含小写字母的字符串 `s`，要选出尽量多的非空子字符串，并满足：任意两个子字符串不重叠；如果某个子字符串包含字符 `ch`，那么 `s` 中所有 `ch` 都必须完整落在这个子字符串内。在数量最多的前提下，还要让所选子字符串的总长度最小。

## 关键思路

这题可以转化成“先找合法最小区间，再做区间贪心选择”。

### 1. 每个字符决定一个必要覆盖范围

先记录每个字符在 `s` 中第一次出现位置 `left[c]` 和最后一次出现位置 `right[c]`。

如果一个合法子字符串包含字符 `c`，它至少必须覆盖 `[left[c], right[c]]`。但覆盖这个范围后，区间中可能又包含别的字符，这些字符的所有出现位置也必须被一起覆盖，所以区间需要继续向右扩张。

从某个字符第一次出现位置 `i` 开始构造区间：

- 初始右端点是 `right[s[i]]`；
- 扫描当前区间 `[i, r]` 中的每个字符 `x`；
- 如果 `left[x] < i`，说明 `x` 在区间左侧还有出现，当前区间不可能合法；
- 否则把右端点扩张为 `max(r, right[x])`。

只有当 `i` 是某个字符的第一次出现位置时，才有必要尝试作为区间左端点。因为若左端点不是任何被包含字符的第一次出现位置，这个子字符串要么非法，要么可以继续收缩。

### 2. 为什么这样做是对的

任意合法子字符串中的每个字符都必须被完整覆盖，所以从它左端点字符的第一次出现位置开始，不断补齐区间内字符的最后出现位置，可以得到包含它所需字符的最小闭包区间。如果扩张过程中发现某字符第一次出现位置在左边界外，则无论怎么扩右端点都无法补齐它，当前左边界非法。

因此，最优答案一定可以从这些最小合法闭包区间中选择。

### 3. 区间选择用结束位置贪心

把候选区间按右端点从小到大排序；右端点相同时，左端点更大的区间更短，优先考虑。随后每次选择当前能选的、结束最早的区间。

结束越早，留给后面区间的空间越多，所以能最大化区间数量；当区间数量相同，优先使用更短的合法闭包区间，可以满足题目要求的最小总长度。

## 复杂度分析

设 `n = s.length`，字符集大小固定为 26。

- 预处理左右端点：`O(n)`；
- 构造候选区间：最多从 26 个起点扩张，整体 `O(26n)`，即 `O(n)`；
- 候选区间排序：最多 26 个区间，视为常数；
- 总时间复杂度：`O(n)`；
- 额外空间复杂度：`O(1)`，不计返回结果。

## 容易踩坑点

1. 不能只取每个字符的 `[first, last]`，还要把区间中其他字符的出现范围一起补齐。
2. 扩张区间时，如果发现某字符的第一次出现位置在当前左端点左侧，当前起点一定非法。
3. 只从字符第一次出现位置尝试构造区间，否则会产生冗余甚至非法的起点。
4. 题目要求“数量最多，其次总长度最小”，不能返回任意最大数量方案。
5. 返回顺序可以任意；下面代码按贪心选择顺序返回。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    vector<string> maxNumOfSubstrings(string s) {
        int n = (int)s.size();
        vector<int> left(26, n), right(26, -1);

        for (int i = 0; i < n; ++i) {
            int c = s[i] - 'a';
            left[c] = min(left[c], i);
            right[c] = i;
        }

        vector<pair<int, int>> intervals;
        for (int i = 0; i < n; ++i) {
            int c = s[i] - 'a';
            if (i != left[c]) continue;

            int r = right[c];
            bool ok = true;
            for (int j = i; j <= r; ++j) {
                int x = s[j] - 'a';
                if (left[x] < i) {
                    ok = false;
                    break;
                }
                r = max(r, right[x]);
            }

            if (ok) intervals.push_back({i, r});
        }

        sort(intervals.begin(), intervals.end(), [](const auto& a, const auto& b) {
            if (a.second != b.second) return a.second < b.second;
            return a.first > b.first;
        });

        vector<string> ans;
        int lastEnd = -1;
        for (auto [l, r] : intervals) {
            if (l > lastEnd) {
                ans.push_back(s.substr(l, r - l + 1));
                lastEnd = r;
            }
        }
        return ans;
    }
};
```

## LeetCode 可提交解法：Python 3

```python
from typing import List

class Solution:
    def maxNumOfSubstrings(self, s: str) -> List[str]:
        n = len(s)
        left = [n] * 26
        right = [-1] * 26

        for i, ch in enumerate(s):
            c = ord(ch) - ord('a')
            left[c] = min(left[c], i)
            right[c] = i

        intervals = []
        for i, ch in enumerate(s):
            c = ord(ch) - ord('a')
            if i != left[c]:
                continue

            r = right[c]
            ok = True
            j = i
            while j <= r:
                x = ord(s[j]) - ord('a')
                if left[x] < i:
                    ok = False
                    break
                r = max(r, right[x])
                j += 1

            if ok:
                intervals.append((i, r))

        intervals.sort(key=lambda p: (p[1], -p[0]))

        ans = []
        last_end = -1
        for l, r in intervals:
            if l > last_end:
                ans.append(s[l:r + 1])
                last_end = r
        return ans
```

## ACM 答题模式

### 自定义输入格式

输入一行小写字母字符串 `s`。

### 自定义输出格式

先输出被选子字符串个数 `k`，之后输出 `k` 行，每行一个被选子字符串。

```text
输入：
adefaddaccc
输出：
3
e
f
ccc
```

### ACM 模式 C++ 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

vector<string> maxNumOfSubstrings(const string& s) {
    int n = (int)s.size();
    vector<int> left(26, n), right(26, -1);

    for (int i = 0; i < n; ++i) {
        int c = s[i] - 'a';
        left[c] = min(left[c], i);
        right[c] = i;
    }

    vector<pair<int, int>> intervals;
    for (int i = 0; i < n; ++i) {
        int c = s[i] - 'a';
        if (i != left[c]) continue;

        int r = right[c];
        bool ok = true;
        for (int j = i; j <= r; ++j) {
            int x = s[j] - 'a';
            if (left[x] < i) {
                ok = false;
                break;
            }
            r = max(r, right[x]);
        }

        if (ok) intervals.push_back({i, r});
    }

    sort(intervals.begin(), intervals.end(), [](const auto& a, const auto& b) {
        if (a.second != b.second) return a.second < b.second;
        return a.first > b.first;
    });

    vector<string> ans;
    int lastEnd = -1;
    for (auto [l, r] : intervals) {
        if (l > lastEnd) {
            ans.push_back(s.substr(l, r - l + 1));
            lastEnd = r;
        }
    }
    return ans;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string s;
    if (!(cin >> s)) return 0;

    vector<string> ans = maxNumOfSubstrings(s);
    cout << ans.size() << '\n';
    for (const string& part : ans) {
        cout << part << '\n';
    }
    return 0;
}
```

### ACM 模式 Python 3 完整代码

```python
import sys


def max_num_of_substrings(s: str) -> list[str]:
    n = len(s)
    left = [n] * 26
    right = [-1] * 26

    for i, ch in enumerate(s):
        c = ord(ch) - ord('a')
        left[c] = min(left[c], i)
        right[c] = i

    intervals = []
    for i, ch in enumerate(s):
        c = ord(ch) - ord('a')
        if i != left[c]:
            continue

        r = right[c]
        ok = True
        j = i
        while j <= r:
            x = ord(s[j]) - ord('a')
            if left[x] < i:
                ok = False
                break
            r = max(r, right[x])
            j += 1

        if ok:
            intervals.append((i, r))

    intervals.sort(key=lambda p: (p[1], -p[0]))

    ans = []
    last_end = -1
    for l, r in intervals:
        if l > last_end:
            ans.append(s[l:r + 1])
            last_end = r
    return ans


def main() -> None:
    s = sys.stdin.readline().strip()
    if not s:
        return

    ans = max_num_of_substrings(s)
    print(len(ans))
    print('\n'.join(ans))


if __name__ == '__main__':
    main()
```

## 本地验证记录

- Python 3 解法已使用官方示例 `adefaddaccc`、`abbaccd` 和边界/sanity 用例 `a`、`abab`、`abc` 验证。
- 当前环境未发现 `g++` 或 `clang++`，因此 C++ 代码未能在本地编译运行；C++ 代码与已验证的 Python 算法逐步对应。
