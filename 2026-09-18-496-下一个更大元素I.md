# 2026-09-18｜LeetCode 496. 下一个更大元素 I

> **题目来源标记：华为技术一面原题**

## 基本信息

- 题号：496
- 题名：下一个更大元素 I
- 英文题名：Next Greater Element I
- 难度：简单
- 标签/考点：数组、哈希表、栈、单调栈
- 题目链接：https://leetcode.cn/problems/next-greater-element-i/

## 题意概括

给定两个没有重复元素的数组 `nums1` 和 `nums2`，其中 `nums1` 是 `nums2` 的子集。

对于 `nums1` 中的每个元素 `x`，需要在 `nums2` 中找到 `x` 所在位置右侧第一个比 `x` 大的元素。如果不存在这样的元素，则答案为 `-1`。

返回 `nums1` 中每个元素对应的下一个更大元素。

## 关键思路：单调递减栈

如果对 `nums1` 的每个元素都去 `nums2` 中向右扫描，时间复杂度会达到 `O(mn)`。更好的做法是先在 `nums2` 中预处理每个元素的“下一个更大元素”。

从左到右遍历 `nums2`，维护一个单调递减栈，栈里保存还没有找到下一个更大元素的值：

1. 当前值为 `value`。
2. 只要栈顶元素小于 `value`，说明 `value` 就是栈顶元素右侧第一个更大的元素。
3. 不断弹出栈顶，并记录 `nextGreater[栈顶] = value`。
4. 最后把当前值入栈。
5. 遍历结束后，栈中剩余元素都没有下一个更大元素，默认答案为 `-1`。

最后根据 `nums1` 的顺序，从哈希表中取答案即可。

## 为什么单调栈正确

栈中的元素从栈底到栈顶保持递减。对于某个被弹出的元素 `x`，当前元素 `value` 是第一个让它被弹出的右侧元素；在 `x` 入栈之后、`value` 到来之前，没有任何元素比 `x` 大，否则 `x` 会更早被弹出。因此 `value` 正是 `x` 的下一个更大元素。

## 复杂度分析

设 `nums1` 长度为 `m`，`nums2` 长度为 `n`：

- 时间复杂度：`O(m + n)`，每个元素最多入栈、出栈一次。
- 空间复杂度：`O(n)`，用于栈和哈希表。

## 容易踩坑点

- “下一个更大元素”必须是右侧第一个更大的元素，不是右侧最大值。
- `nums1` 的输出顺序要和 `nums1` 一致，不能按 `nums2` 顺序输出。
- 题目保证元素互不重复，并且 `nums1` 是 `nums2` 的子集，因此可以直接用元素值作为哈希表键。
- 栈中保存元素值即可，不一定要保存下标。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {
        unordered_map<int, int> nextGreater;
        vector<int> stack;

        for (int value : nums2) {
            while (!stack.empty() && stack.back() < value) {
                nextGreater[stack.back()] = value;
                stack.pop_back();
            }
            stack.push_back(value);
        }

        vector<int> answer;
        answer.reserve(nums1.size());
        for (int value : nums1) {
            if (nextGreater.count(value)) {
                answer.push_back(nextGreater[value]);
            } else {
                answer.push_back(-1);
            }
        }
        return answer;
    }
};
```

## LeetCode 可提交解法：Python 3

```python
class Solution:
    def nextGreaterElement(self, nums1: list[int], nums2: list[int]) -> list[int]:
        next_greater = {}
        stack = []

        for value in nums2:
            while stack and stack[-1] < value:
                next_greater[stack.pop()] = value
            stack.append(value)

        return [next_greater.get(value, -1) for value in nums1]
```

## ACM 答题模式

### 输入格式

第一行输入两个整数 `m` 和 `n`，分别表示 `nums1` 和 `nums2` 的长度。

第二行输入 `m` 个整数，表示 `nums1`。

第三行输入 `n` 个整数，表示 `nums2`。

```text
m n
nums1[0] nums1[1] ... nums1[m-1]
nums2[0] nums2[1] ... nums2[n-1]
```

### 输出格式

输出 `m` 个整数，表示 `nums1` 中每个元素的下一个更大元素；不存在则为 `-1`。

### ACM 模式 C++ 完整代码

```cpp
#include <iostream>
#include <unordered_map>
#include <vector>
using namespace std;

vector<int> nextGreaterElement(const vector<int>& nums1, const vector<int>& nums2) {
    unordered_map<int, int> nextGreater;
    vector<int> stack;

    for (int value : nums2) {
        while (!stack.empty() && stack.back() < value) {
            nextGreater[stack.back()] = value;
            stack.pop_back();
        }
        stack.push_back(value);
    }

    vector<int> answer;
    answer.reserve(nums1.size());
    for (int value : nums1) {
        auto it = nextGreater.find(value);
        answer.push_back(it == nextGreater.end() ? -1 : it->second);
    }
    return answer;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int m, n;
    cin >> m >> n;

    vector<int> nums1(m), nums2(n);
    for (int& value : nums1) {
        cin >> value;
    }
    for (int& value : nums2) {
        cin >> value;
    }

    vector<int> answer = nextGreaterElement(nums1, nums2);
    for (int i = 0; i < static_cast<int>(answer.size()); ++i) {
        if (i > 0) cout << ' ';
        cout << answer[i];
    }
    cout << '\n';
    return 0;
}
```

### ACM 模式 Python 3 完整代码

```python
import sys


def next_greater_element(nums1: list[int], nums2: list[int]) -> list[int]:
    next_greater = {}
    stack = []

    for value in nums2:
        while stack and stack[-1] < value:
            next_greater[stack.pop()] = value
        stack.append(value)

    return [next_greater.get(value, -1) for value in nums1]


def main() -> None:
    data = list(map(int, sys.stdin.buffer.read().split()))
    if not data:
        return

    m, n = data[0], data[1]
    nums1 = data[2:2 + m]
    nums2 = data[2 + m:2 + m + n]
    answer = next_greater_element(nums1, nums2)
    print(" ".join(map(str, answer)))


if __name__ == "__main__":
    main()
```

## 示例与自测

### 示例 1

```text
输入：
3 4
4 1 2
1 3 4 2

输出：
-1 3 -1
```

### 示例 2

```text
输入：
2 4
2 4
1 2 3 4

输出：
3 -1
```

### 边界用例：严格递减

```text
输入：
3 3
3 2 1
3 2 1

输出：
-1 -1 -1
```

### 边界用例：严格递增

```text
输入：
3 3
1 2 3
1 2 3

输出：
2 3 -1
```
