# 1401. 圆和矩形是否有重叠

- 日期：2026-09-19
- 题号：1401
- 题名：圆和矩形是否有重叠 / Circle and Rectangle Overlapping
- 难度：Medium（中等）
- 标签/考点：几何、数学、最近点、边界判断
- 题目链接：https://leetcode.cn/problems/circle-and-rectangle-overlapping/
- 官方来源：力扣 GraphQL todayRecord（date=2026-09-19）。同时尝试 LeetCode activeDailyCodingChallengeQuestion，返回 date=2026-09-18 的 #1520，与中国本地日期不一致，因此本篇采用力扣当天记录。

## 题意概括

给定一个圆 `(radius, xCenter, yCenter)` 和一个坐标轴平行的矩形 `(x1, y1, x2, y2)`，判断二者是否存在至少一个公共点。圆和矩形的边界都算作内部，因此相切也要返回 `true`。

## 关键思路

把问题转化为：矩形中距离圆心最近的点，到圆心的距离是否不超过半径。

因为矩形与坐标轴平行，所以最近点可以按坐标分别确定：

1. 对 x 坐标，如果 `xCenter < x1`，最近的 x 是 `x1`；如果 `xCenter > x2`，最近的 x 是 `x2`；否则圆心的 x 已经落在矩形横向范围内，最近的 x 就是 `xCenter`。
2. y 坐标同理，在 `[y1, y2]` 中夹住 `yCenter`。
3. 设最近点为 `(closestX, closestY)`，只需判断平方距离：`(xCenter - closestX)^2 + (yCenter - closestY)^2 <= radius^2`。

## 为什么这样做

矩形内部所有点的 x 坐标都在 `[x1, x2]`，y 坐标都在 `[y1, y2]`。对任意一个坐标轴，如果圆心坐标已经在区间内，该方向距离可以取到 0；否则离区间最近的一定是较近的端点。两个方向独立取最近值，得到的点就是整个矩形中距离圆心最近的点。

如果这个最近点都在圆外，那么矩形中其他点离圆心只会更远，不可能重叠；如果最近点在圆内或圆上，则这个点同时属于圆和矩形，二者重叠。

## 复杂度分析

- 时间复杂度：`O(1)`，只做常数次比较和计算。
- 空间复杂度：`O(1)`。

## 容易踩坑点

1. 相切算重叠，所以判断条件是 `<= radius * radius`，不是 `<`。
2. 不要只检查矩形四个顶点：圆心可能正对矩形边或在矩形内部。
3. C++ 中建议用 `long long` 计算平方，虽然本题约束下 `int` 也能承受，但使用长整型更稳。
4. 题目保证 `x1 < x2` 且 `y1 < y2`，无需额外交换端点。

## LeetCode 可提交解法：C++

```cpp
class Solution {
public:
    bool checkOverlap(int radius, int xCenter, int yCenter, int x1, int y1, int x2, int y2) {
        int closestX = min(max(xCenter, x1), x2);
        int closestY = min(max(yCenter, y1), y2);

        long long dx = 1LL * xCenter - closestX;
        long long dy = 1LL * yCenter - closestY;
        long long r = radius;

        return dx * dx + dy * dy <= r * r;
    }
};
```

## LeetCode 可提交解法：Python 3

```python
class Solution:
    def checkOverlap(self, radius: int, xCenter: int, yCenter: int, x1: int, y1: int, x2: int, y2: int) -> bool:
        closest_x = min(max(xCenter, x1), x2)
        closest_y = min(max(yCenter, y1), y2)

        dx = xCenter - closest_x
        dy = yCenter - closest_y

        return dx * dx + dy * dy <= radius * radius
```

## ACM 答题模式

### 自定义输入格式

输入 7 个整数：

```text
radius xCenter yCenter x1 y1 x2 y2
```

输出 `true` 或 `false`，表示圆和矩形是否有重叠。

### ACM C++ 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

bool checkOverlap(int radius, int xCenter, int yCenter, int x1, int y1, int x2, int y2) {
    int closestX = min(max(xCenter, x1), x2);
    int closestY = min(max(yCenter, y1), y2);

    long long dx = 1LL * xCenter - closestX;
    long long dy = 1LL * yCenter - closestY;
    long long r = radius;

    return dx * dx + dy * dy <= r * r;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int radius, xCenter, yCenter, x1, y1, x2, y2;
    if (!(cin >> radius >> xCenter >> yCenter >> x1 >> y1 >> x2 >> y2)) {
        return 0;
    }

    bool ans = checkOverlap(radius, xCenter, yCenter, x1, y1, x2, y2);
    cout << (ans ? "true" : "false") << endl;
    return 0;
}
```

### ACM Python 3 完整代码

```python
import sys


def check_overlap(radius: int, x_center: int, y_center: int, x1: int, y1: int, x2: int, y2: int) -> bool:
    closest_x = min(max(x_center, x1), x2)
    closest_y = min(max(y_center, y1), y2)

    dx = x_center - closest_x
    dy = y_center - closest_y

    return dx * dx + dy * dy <= radius * radius


def main() -> None:
    nums = list(map(int, sys.stdin.read().split()))
    if len(nums) < 7:
        return

    radius, x_center, y_center, x1, y1, x2, y2 = nums[:7]
    ans = check_overlap(radius, x_center, y_center, x1, y1, x2, y2)
    print("true" if ans else "false")


if __name__ == "__main__":
    main()
```

## 示例与边界验证

| 用例 | 输入 | 期望输出 | 说明 |
|---|---|---|---|
| 示例 1 | `1 0 0 1 -1 3 1` | `true` | 圆与矩形在 `(1, 0)` 相交 |
| 示例 2 | `1 1 1 1 -3 2 -1` | `false` | 最近点到圆心距离大于半径 |
| 示例 3 | `1 0 0 -1 0 0 1` | `true` | 边界相交 |
| 边界 1 | `1 0 0 2 2 3 3` | `false` | 完全分离 |
| 边界 2 | `1 2 2 0 0 5 5` | `true` | 圆心在矩形内部 |
| 边界 3 | `5 0 0 3 4 10 10` | `true` | 与矩形角点相切 |

本地验证：Python 3 代码已通过以上 6 组用例；当前环境未检测到可用 C++ 编译器，因此 C++ 代码未能本地编译运行。
