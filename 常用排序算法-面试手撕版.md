# 常用排序算法：面试手撕版

> 目标：能在白板或在线编辑器中快速写对，并能解释时间复杂度、空间复杂度、稳定性和适用场景。

## 1. 一张表快速记忆

设待排序元素个数为 `n`，数据值域大小为 `k`。

| 排序算法 | 最好时间 | 平均时间 | 最坏时间 | 额外空间 | 稳定性 | 核心特点 |
|---|---:|---:|---:|---:|---|---|
| 冒泡排序 | `O(n)` | `O(n²)` | `O(n²)` | `O(1)` | 稳定 | 相邻逆序就交换，可提前结束 |
| 选择排序 | `O(n²)` | `O(n²)` | `O(n²)` | `O(1)` | 不稳定 | 每轮选择最小值放到前面 |
| 插入排序 | `O(n)` | `O(n²)` | `O(n²)` | `O(1)` | 稳定 | 维护有序前缀，适合近乎有序数据 |
| 希尔排序 | 与增量有关 | 常见约 `O(n^1.3)` | 常按 `O(n²)` 记 | `O(1)` | 不稳定 | 分组插入排序，逐步缩小间隔 |
| 归并排序 | `O(n log n)` | `O(n log n)` | `O(n log n)` | `O(n)` | 稳定 | 分治；链表排序也很适合 |
| 快速排序 | `O(n log n)` | `O(n log n)` | `O(n²)` | 平均 `O(log n)` | 不稳定 | 原地分区；工程中常用 |
| 堆排序 | `O(n log n)` | `O(n log n)` | `O(n log n)` | `O(1)` | 不稳定 | 原地且最坏复杂度有保证 |
| 计数排序 | `O(n+k)` | `O(n+k)` | `O(n+k)` | `O(k)` | 可稳定 | 只适合整数且值域不能太大 |
| 桶排序 | 取决于桶内排序 | 平均 `O(n+k)` | `O(n²)` | `O(n+k)` | 取决于桶内排序 | 适合分布较均匀的数据 |
| 基数排序 | `O(d(n+r))` | `O(d(n+r))` | `O(d(n+r))` | `O(n+r)` | 稳定 | 按数位排序；`d` 为位数，`r` 为基数 |

> 稳定排序：相等元素排序后仍保持原有相对顺序。只有在元素带有其他字段时，稳定性才真正有意义。

## 2. 面试时怎么选

- **默认手撕数组排序**：三路快速排序。代码短，对重复元素友好。
- **要求稳定、最坏 `O(n log n)`**：归并排序。
- **要求原地、最坏 `O(n log n)`**：堆排序。
- **数据规模小或基本有序**：插入排序。
- **整数值域很小**：计数排序。
- **非负整数、位数有限**：基数排序。
- **单链表排序**：优先归并排序，拆分和合并都不需要随机访问。

面试解释建议遵循四句话：

1. 我维护的区间或不变量是什么；
2. 每轮如何缩小未排序区间；
3. 为什么最终有序；
4. 时间、空间和稳定性分别是什么。

---

## 3. 比较类排序

### 3.1 冒泡排序

**不变量**：完成第 `i` 轮后，末尾 `i` 个元素已经就位。

```cpp
void bubbleSort(vector<int>& a) {
    int n = (int)a.size();
    for (int end = n - 1; end > 0; --end) {
        bool swapped = false;
        for (int i = 0; i < end; ++i) {
            if (a[i] > a[i + 1]) {
                swap(a[i], a[i + 1]);
                swapped = true;
            }
        }
        if (!swapped) break;
    }
}
```

```python
def bubble_sort(nums: list[int]) -> None:
    for end in range(len(nums) - 1, 0, -1):
        swapped = False
        for i in range(end):
            if nums[i] > nums[i + 1]:
                nums[i], nums[i + 1] = nums[i + 1], nums[i]
                swapped = True
        if not swapped:
            break
```

**易错点**：内层只遍历到 `end - 1`，因为会访问 `i + 1`；必须用 `>` 而不是 `>=` 才能保持稳定。

### 3.2 选择排序

**不变量**：进入第 `i` 轮时，`[0, i)` 已经有序且都是全局最小的元素。

```cpp
void selectionSort(vector<int>& a) {
    int n = (int)a.size();
    for (int i = 0; i < n; ++i) {
        int minIndex = i;
        for (int j = i + 1; j < n; ++j) {
            if (a[j] < a[minIndex]) minIndex = j;
        }
        if (minIndex != i) swap(a[i], a[minIndex]);
    }
}
```

```python
def selection_sort(nums: list[int]) -> None:
    n = len(nums)
    for i in range(n):
        min_index = i
        for j in range(i + 1, n):
            if nums[j] < nums[min_index]:
                min_index = j
        if min_index != i:
            nums[i], nums[min_index] = nums[min_index], nums[i]
```

**为什么不稳定**：把最小值直接换到前面时，可能越过与当前位置相等的元素，破坏原相对顺序。

### 3.3 插入排序

**不变量**：进入第 `i` 轮时，`[0, i)` 已经有序；把 `a[i]` 插入正确位置。

```cpp
void insertionSort(vector<int>& a) {
    int n = (int)a.size();
    for (int i = 1; i < n; ++i) {
        int value = a[i];
        int j = i - 1;
        while (j >= 0 && a[j] > value) {
            a[j + 1] = a[j];
            --j;
        }
        a[j + 1] = value;
    }
}
```

```python
def insertion_sort(nums: list[int]) -> None:
    for i in range(1, len(nums)):
        value = nums[i]
        j = i - 1
        while j >= 0 and nums[j] > value:
            nums[j + 1] = nums[j]
            j -= 1
        nums[j + 1] = value
```

**易错点**：先保存 `value`；循环结束后插入位置是 `j + 1`；比较使用 `>` 可保持稳定。

### 3.4 希尔排序

希尔排序是“带间隔的插入排序”。间隔逐步缩小到 `1`，最后一轮就是普通插入排序。

```cpp
void shellSort(vector<int>& a) {
    int n = (int)a.size();
    for (int gap = n / 2; gap > 0; gap /= 2) {
        for (int i = gap; i < n; ++i) {
            int value = a[i];
            int j = i;
            while (j >= gap && a[j - gap] > value) {
                a[j] = a[j - gap];
                j -= gap;
            }
            a[j] = value;
        }
    }
}
```

```python
def shell_sort(nums: list[int]) -> None:
    gap = len(nums) // 2
    while gap > 0:
        for i in range(gap, len(nums)):
            value = nums[i]
            j = i
            while j >= gap and nums[j - gap] > value:
                nums[j] = nums[j - gap]
                j -= gap
            nums[j] = value
        gap //= 2
```

**易错点**：移动步长是 `gap`，不是 `1`；希尔排序的复杂度与增量序列有关，面试中不要笼统声称一定是 `O(n log n)`。

### 3.5 归并排序

**思路**：先分别排好左右两半，再用双指针把两个有序区间合并。

```cpp
void mergeSort(vector<int>& a) {
    if (a.empty()) return;
    vector<int> temp(a.size());

    auto sortRange = [&](auto&& self, int left, int right) -> void {
        if (left >= right) return;
        int mid = left + (right - left) / 2;
        self(self, left, mid);
        self(self, mid + 1, right);

        int i = left, j = mid + 1, k = left;
        while (i <= mid && j <= right) {
            if (a[i] <= a[j]) temp[k++] = a[i++];
            else temp[k++] = a[j++];
        }
        while (i <= mid) temp[k++] = a[i++];
        while (j <= right) temp[k++] = a[j++];
        for (int p = left; p <= right; ++p) a[p] = temp[p];
    };

    sortRange(sortRange, 0, (int)a.size() - 1);
}
```

```python
def merge_sort(nums: list[int]) -> None:
    temp = [0] * len(nums)

    def sort_range(left: int, right: int) -> None:
        if left >= right:
            return
        mid = left + (right - left) // 2
        sort_range(left, mid)
        sort_range(mid + 1, right)

        i, j, k = left, mid + 1, left
        while i <= mid and j <= right:
            if nums[i] <= nums[j]:
                temp[k] = nums[i]
                i += 1
            else:
                temp[k] = nums[j]
                j += 1
            k += 1
        while i <= mid:
            temp[k] = nums[i]
            i += 1
            k += 1
        while j <= right:
            temp[k] = nums[j]
            j += 1
            k += 1
        nums[left:right + 1] = temp[left:right + 1]

    if nums:
        sort_range(0, len(nums) - 1)
```

**易错点**：

- 中点写成 `left + (right - left) / 2`，避免某些语言中 `left + right` 溢出；
- 左边相等时先取左侧元素，即使用 `<=`，才能保持稳定；
- 合并后必须复制回原数组。

### 3.6 三路快速排序（推荐手撕）

维护三个区间：

- `[left, less)`：小于基准值；
- `[less, i)`：等于基准值；
- `(greater, right]`：大于基准值。

未处理区间是 `[i, greater]`。重复元素多时，三路分区比普通二路分区更稳健。

```cpp
void quickSort(vector<int>& a) {
    auto sortRange = [&](auto&& self, int left, int right) -> void {
        if (left >= right) return;
        int pivot = a[left + (right - left) / 2];
        int less = left, i = left, greater = right;

        while (i <= greater) {
            if (a[i] < pivot) {
                swap(a[less++], a[i++]);
            } else if (a[i] > pivot) {
                swap(a[i], a[greater--]);
            } else {
                ++i;
            }
        }

        self(self, left, less - 1);
        self(self, greater + 1, right);
    };

    if (!a.empty()) sortRange(sortRange, 0, (int)a.size() - 1);
}
```

```python
def quick_sort(nums: list[int]) -> None:
    def sort_range(left: int, right: int) -> None:
        if left >= right:
            return
        pivot = nums[left + (right - left) // 2]
        less, i, greater = left, left, right

        while i <= greater:
            if nums[i] < pivot:
                nums[less], nums[i] = nums[i], nums[less]
                less += 1
                i += 1
            elif nums[i] > pivot:
                nums[i], nums[greater] = nums[greater], nums[i]
                greater -= 1
            else:
                i += 1

        sort_range(left, less - 1)
        sort_range(greater + 1, right)

    if nums:
        sort_range(0, len(nums) - 1)
```

**易错点**：与右侧交换后不能立刻 `i++`，因为新换到 `i` 的元素还没有检查；快速排序最坏仍可能退化为 `O(n²)`，工程实现通常会随机化基准值或使用 introsort。

### 3.7 堆排序

升序排序使用**大顶堆**：每次把堆顶最大值交换到当前末尾，再恢复剩余区间的大顶堆性质。

```cpp
void heapSort(vector<int>& a) {
    int n = (int)a.size();

    auto siftDown = [&](int root, int heapSize) {
        while (true) {
            int child = root * 2 + 1;
            if (child >= heapSize) break;
            if (child + 1 < heapSize && a[child + 1] > a[child]) ++child;
            if (a[root] >= a[child]) break;
            swap(a[root], a[child]);
            root = child;
        }
    };

    for (int i = n / 2 - 1; i >= 0; --i) siftDown(i, n);
    for (int end = n - 1; end > 0; --end) {
        swap(a[0], a[end]);
        siftDown(0, end);
    }
}
```

```python
def heap_sort(nums: list[int]) -> None:
    n = len(nums)

    def sift_down(root: int, heap_size: int) -> None:
        while True:
            child = root * 2 + 1
            if child >= heap_size:
                break
            if child + 1 < heap_size and nums[child + 1] > nums[child]:
                child += 1
            if nums[root] >= nums[child]:
                break
            nums[root], nums[child] = nums[child], nums[root]
            root = child

    for i in range(n // 2 - 1, -1, -1):
        sift_down(i, n)
    for end in range(n - 1, 0, -1):
        nums[0], nums[end] = nums[end], nums[0]
        sift_down(0, end)
```

**易错点**：`heapSize` 表示左闭右开区间 `[0, heapSize)`；升序必须建大顶堆；建堆从最后一个非叶子节点 `n / 2 - 1` 开始。

---

## 4. 非比较类排序

### 4.1 计数排序

下面的版本支持负数。值域过大时不应使用计数排序。

```cpp
void countingSort(vector<int>& a) {
    if (a.empty()) return;
    auto [minIt, maxIt] = minmax_element(a.begin(), a.end());
    int minValue = *minIt, maxValue = *maxIt;
    long long range = 1LL * maxValue - minValue + 1;
    if (range > 2000000) {
        throw invalid_argument("value range is too large for counting sort");
    }

    vector<int> count((size_t)range, 0);
    for (int value : a) ++count[value - minValue];

    int index = 0;
    for (long long i = 0; i < range; ++i) {
        while (count[i]-- > 0) a[index++] = (int)(i + minValue);
    }
}
```

```python
def counting_sort(nums: list[int]) -> None:
    if not nums:
        return
    min_value, max_value = min(nums), max(nums)
    value_range = max_value - min_value + 1
    if value_range > 2_000_000:
        raise ValueError("value range is too large for counting sort")

    count = [0] * value_range
    for value in nums:
        count[value - min_value] += 1

    index = 0
    for offset, frequency in enumerate(count):
        for _ in range(frequency):
            nums[index] = offset + min_value
            index += 1
```

**易错点**：数组下标需要减去最小值；空间取决于值域 `k` 而不是元素个数 `n`。上面的“直接回填”版本只排序整数值；如果排序带附加字段的记录，需要使用前缀和与反向遍历实现稳定版本。

### 4.2 桶排序

经典面试版本通常假设数据位于 `[0, 1]`，将元素按数值范围分桶，再分别排序。

```cpp
void bucketSort(vector<double>& a) {
    int n = (int)a.size();
    if (n < 2) return;
    vector<vector<double>> buckets(n);

    for (double value : a) {
        if (value < 0.0 || value > 1.0) {
            throw invalid_argument("bucket sort expects values in [0, 1]");
        }
        int index = min(n - 1, (int)(value * n));
        buckets[index].push_back(value);
    }

    int index = 0;
    for (auto& bucket : buckets) {
        sort(bucket.begin(), bucket.end());
        for (double value : bucket) a[index++] = value;
    }
}
```

```python
def bucket_sort(nums: list[float]) -> None:
    n = len(nums)
    if n < 2:
        return
    buckets: list[list[float]] = [[] for _ in range(n)]

    for value in nums:
        if value < 0.0 or value > 1.0:
            raise ValueError("bucket sort expects values in [0, 1]")
        index = min(n - 1, int(value * n))
        buckets[index].append(value)

    nums.clear()
    for bucket in buckets:
        bucket.sort()
        nums.extend(bucket)
```

**易错点**：需要先说清数据分布和范围；如果所有数据都进入同一个桶，复杂度会退化到桶内排序的复杂度。

### 4.3 LSD 基数排序

下面使用十进制，从最低位到最高位依次做稳定的计数排序，仅处理**非负整数**。

```cpp
void radixSort(vector<int>& a) {
    if (a.empty()) return;
    if (*min_element(a.begin(), a.end()) < 0) {
        throw invalid_argument("this radix sort only supports non-negative integers");
    }

    int maxValue = *max_element(a.begin(), a.end());
    vector<int> output(a.size());

    for (long long exp = 1; maxValue / exp > 0; exp *= 10) {
        int count[10] = {};
        for (int value : a) ++count[(value / exp) % 10];
        for (int i = 1; i < 10; ++i) count[i] += count[i - 1];
        for (int i = (int)a.size() - 1; i >= 0; --i) {
            int digit = (a[i] / exp) % 10;
            output[--count[digit]] = a[i];
        }
        a.swap(output);
    }
}
```

```python
def radix_sort(nums: list[int]) -> None:
    if not nums:
        return
    if min(nums) < 0:
        raise ValueError("this radix sort only supports non-negative integers")

    max_value = max(nums)
    output = [0] * len(nums)
    exp = 1
    while max_value // exp > 0:
        count = [0] * 10
        for value in nums:
            count[(value // exp) % 10] += 1
        for i in range(1, 10):
            count[i] += count[i - 1]
        for i in range(len(nums) - 1, -1, -1):
            digit = (nums[i] // exp) % 10
            count[digit] -= 1
            output[count[digit]] = nums[i]
        nums[:] = output
        exp *= 10
```

**易错点**：每一位的排序必须稳定，因此回填 `output` 时从右向左扫描；负数需要额外拆分处理，不能直接套用这个版本。

---

## 5. ACM 答题模式：三路快速排序

### 输入格式

```text
n
x1 x2 ... xn
```

- `0 <= n`
- 第二行包含 `n` 个整数；当 `n = 0` 时可以省略第二行。

### 输出格式

按升序输出排序后的整数，以空格分隔。

### 样例

输入：

```text
8
5 1 3 3 -2 8 0 3
```

输出：

```text
-2 0 1 3 3 3 5 8
```

### C++ 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

void quickSort(vector<int>& a, int left, int right) {
    if (left >= right) return;

    int pivot = a[left + (right - left) / 2];
    int less = left, i = left, greater = right;
    while (i <= greater) {
        if (a[i] < pivot) {
            swap(a[less++], a[i++]);
        } else if (a[i] > pivot) {
            swap(a[i], a[greater--]);
        } else {
            ++i;
        }
    }

    quickSort(a, left, less - 1);
    quickSort(a, greater + 1, right);
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    if (!(cin >> n)) return 0;
    vector<int> a(n);
    for (int& value : a) cin >> value;

    if (!a.empty()) quickSort(a, 0, n - 1);

    for (int i = 0; i < n; ++i) {
        if (i) cout << ' ';
        cout << a[i];
    }
    cout << '\n';
    return 0;
}
```

### Python 3 完整代码

```python
import sys


def quick_sort(nums: list[int]) -> None:
    def sort_range(left: int, right: int) -> None:
        if left >= right:
            return

        pivot = nums[left + (right - left) // 2]
        less, i, greater = left, left, right
        while i <= greater:
            if nums[i] < pivot:
                nums[less], nums[i] = nums[i], nums[less]
                less += 1
                i += 1
            elif nums[i] > pivot:
                nums[i], nums[greater] = nums[greater], nums[i]
                greater -= 1
            else:
                i += 1

        sort_range(left, less - 1)
        sort_range(greater + 1, right)

    if nums:
        sort_range(0, len(nums) - 1)


def main() -> None:
    data = list(map(int, sys.stdin.buffer.read().split()))
    if not data:
        return
    n = data[0]
    nums = data[1:1 + n]
    quick_sort(nums)
    print(*nums)


if __name__ == "__main__":
    main()
```

---

## 6. 高频追问

### 为什么快速排序通常比堆排序快？

快速排序的分区过程主要是顺序访问数组，缓存局部性更好；堆排序会在树的父子节点之间跳跃访问，缓存命中率较低。两者平均都是 `O(n log n)`，但常数不同。

### 为什么建堆是 `O(n)`，不是 `O(n log n)`？

靠近叶子的节点高度很低，数量却最多；把每个节点的下沉代价按高度加总后是一个收敛级数，因此总成本为 `O(n)`。

### `std::sort` / Java `Arrays.sort` / Python `sort` 背后是什么？

- C++ `std::sort` 通常使用 introsort：快速排序为主，递归过深时切到堆排序，小区间使用插入排序。
- Java 对基本类型数组通常使用双轴快速排序，对对象数组使用稳定的 TimSort。
- Python 的 `list.sort()` 和 `sorted()` 使用稳定的 TimSort，善于利用已有有序片段。

### 排序算法的理论下界是什么？

任何基于比较的通用排序，在最坏情况下都需要 `Ω(n log n)` 次比较。计数、桶、基数排序能突破这一界限，是因为它们使用了键的值域或数位信息，并非只进行元素间比较。

## 7. 手撕检查清单

- [ ] 空数组和单元素数组是否能正确处理？
- [ ] 左右边界是闭区间还是左闭右开，是否全程一致？
- [ ] 循环是否可能越界或死循环？
- [ ] 快排与归并递归区间是否严格缩小？
- [ ] 相等元素如何处理？是否符合稳定性要求？
- [ ] 复杂度是否包含递归栈或辅助数组？
- [ ] 计数、桶、基数排序是否明确了数据范围限制？
- [ ] ACM 模式是否处理 `n = 0`、重复数、负数和已经有序的数据？
